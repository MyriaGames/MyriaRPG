<Page x:Class="Myria.Wpf.View.Pages.Page_Loading"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Loading">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="360" MaxWidth="560">

            <TextBlock Text="Myria.Wpf"
                       FontSize="32" FontWeight="Bold"
                       Foreground="{DynamicResource Brush.Foreground}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,6"/>

            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="13"
                       Foreground="{DynamicResource Brush.Muted}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,28"/>

            <!-- Step list -->
            <ItemsControl ItemsSource="{Binding Steps}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Label}"
                                       Foreground="{DynamicResource Brush.Foreground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Status}"
                                       Foreground="{Binding StatusColor}"
                                       FontSize="13"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Mod warnings (visible only when there are non-fatal issues) -->
            <Border Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVis}}"
                    Background="{DynamicResource Brush.PanelDark}"
                    BorderBrush="#FFCC00"
                    BorderThickness="1"
                    CornerRadius="3"
                    Margin="0,20,0,0"
                    Padding="12,10">
                <StackPanel>
                    <TextBlock Text="The following mods could not be activated:"
                               Foreground="#FFCC00"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <ScrollViewer MaxHeight="160"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <TextBlock Text="{Binding WarningsSummary}"
                                   Foreground="{DynamicResource Brush.Foreground}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   FontFamily="Consolas"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- OK button — only shown when user acknowledgement is needed -->
            <Button Content="OK"
                    Visibility="{Binding OkButtonVisible, Converter={StaticResource BoolToVis}}"
                    Command="{Binding OkCommand}"
                    HorizontalAlignment="Center"
                    Margin="0,16,0,0"
                    Padding="32,8"
                    FontSize="14"
                    Background="{DynamicResource Brush.BlueActive}"
                    Foreground="{DynamicResource Brush.Foreground}"
                    BorderThickness="0"
                    Cursor="Hand"/>

        </StackPanel>
    </Grid>
</Page>
