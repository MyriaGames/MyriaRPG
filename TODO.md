# MyriaRPG — Project ToDo

Legend: ✅ Done | 🔧 In Progress | ⬜ Not Started | ⚠️ Partial / Needs Polish

**Release split (2026-09-10):** every open (non-✅) item below is sorted into exactly one of the
two lists right below — bugs get fixed on the current v0.2.x line and shipped as soon as they're
ready; content/features/polish wait for v0.3.0, which only starts once v0.2.x has gone a full
playtest with zero confirmed bugs. Sorting rule: "would a friend casually playing (not looking for
edge cases) run into this, or does it require deliberately poking at something (a modified client,
an unusual action sequence, a content gap that's expected to be sparse right now)?" — the former is
a bug, the latter is deferred. Each row keeps its original ID/section as `Ref` so the full detail
(code pointers, history) is still in the matching section further down; this isn't a duplicate list,
the rows still live in their original tables too.

## 🐛 Bug Fixes — v0.2.x (work this list first, priority order)

Refilled 2026-09-10 with 5 newly user-reported items. All 5 (#40–#44) were resolved, then items 45–48 were reported during a fresh playtest — all now resolved. This list is empty, clearing the way for v0.3.0 content work once a playtest confirms no regressions.

| # | Ref | Item | Notes |
|---|-----|------|-------|

## 🧩 Deferred — v0.3.0 Content Backlog (do not start before the list above is empty)

| # | Ref | Item | Why it's deferred, not a bug |
|---|-----|------|-------------------------------|
| 1 | [MP21](#track-b--mirror-method-server-authority) | Equip/unequip stat re-derivation | Anti-cheat hardening against a modified client, not a correctness issue for normal play — the actual desync risk here was already closed (see MP21's own note) |
| 2 | [25](#polish--bugs--cleanup) | Multiplayer anti-cheat / server authority | Pointer entry, same reasoning as MP21 above |
| 3 | [Q14](#quest-system--detailed-design) | Quest list category grouping (Main/Side/Faction) | Pure UI/UX enhancement, nothing broken without it |
| 4 | [23](#polish--bugs--cleanup) | Rename `QuestListPageViewModelcs.cs` | Cosmetic filename typo, zero functional effect |
| 5 | [19](#in-progress--needs-work) | World data — caves/dungeons/forests/cities sparse | Known-sparse content stubs, not a defect |
| 6 | [CO4–CO7](#content--lore) | Gathering spots, story locations, MP-era content | New content/world-building |
| 7 | [LO1–LO3](#language--lore-systems) | Myralic font refresh, Lexica system, multiple languages | New systems/content, font asset exists but needs a redraw to match the current script |
| 8 | [AR1, AR2, AR4](#art--visual-polish-wpf-only) | Backgrounds, icons, theme rework | Visual polish |
| 9 | [27, 28, 29, 31 (Future/Backlog)](#future--backlog) | More race/class profiles, more world content, PvP, achievements | Net-new systems/content, no design started |

---

## Core Systems

| # | Feature | Status | Notes |
|---|---------|--------|-------|
| 1 | Jobs system | ✅ Done | Doc audit 2026-09-10: every J1–J23 item in the detail section below is ✅ Done — this summary row was stale (said "several mechanics still missing") |
| 2 | Quest system | ✅ Done | Doc audit 2026-09-10: Q1–Q13 all done; Q14 (category grouping in the quest list) fixed 2026-09-11 — all Q1–Q14 now done, see Quest detail section below |
| 3 | NPC interaction panels | ✅ Done | Dialog, Shop, Upgrade, Craft, Class, JobMaster, QuestDialog |
| 4 | Inventory & Items | ✅ Done | 200+ items, multi-tab inventory, drag-drop equip |
| 5 | Shop / Crafting / Upgrade | ✅ Done | Buy/sell, recipe crafting, stat upgrades |
| 6 | Skill system | ✅ Done | Learn, slot, combine skills; SkillDetailWindow |
| 7 | Character creation | ✅ Done | Doc audit 2026-09-10: CL1 (class selection) is done — `Page_CharacterCreation.xaml` has a class picker; this row was stale (said "class selection missing"), see Class detail section |
| 8 | Character selection / save | ✅ Done | Server-persisted via SqlCharacterRepository |
| 9 | Map / Room navigation | ✅ Done | M7 (NPC hover tooltip) fixed 2026-09-10 — all M1-M8 now done, see Map detail section below |
| 10 | Localization (EN / DE) | ✅ Done | Auto-wired via [LocalizedKey] attribute |
| 11 | Theming (Light / Dark) | ✅ Done | Resource-dictionary swap at runtime |
| 12 | Settings persistence | ✅ Done | Visuals, keybindings, language |
| 13 | Combat (singleplayer) | ✅ Done | Client-side encounter simulation |
| 14 | Gathering spots | ✅ Done | GatherService integrated with room system |

---

## Jobs System — Detailed Design

What is already done: 3-aspect XP model (Skill / Knowledge / Fame), level math, 1-week switch cooldown,
Fame sell-value multiplier, Skill gather/craft multiplier formulas, JobMaster NPC panel, XP grant methods.

| # | Item | Status | Notes |
|---|------|--------|-------|
| J1 | Skill XP from crafting/upgrading/gathering (not only active job) | ✅ Done | +50 % bonus when active, always granted |
| J2 | Fame XP only while job is active | ✅ Done | GrantFameXp checks ActiveJobId |
| J3 | Job switch 1-week cooldown | ✅ Done | JobManager.JobChangeCooldown = 7 days |
| J4 | Fame sell-value bonus | ✅ Done | JobManager.GetSellValue applies fame multiplier |
| J5 | Skill multiplier formula | ✅ Done | JobXpService.GetSkillMultiplier / ApplyGatherMultiplier |
| J6 | Knowledge gather-limit and upgrade-limit formulas | ✅ Done | GetGatherLimitBonus / GetMaxUpgradeLevel in JobXpService |
| J7 | Quest entity: `RequiredActiveJobId` field | ✅ Done | Added to Quest entity + Clone(); QuestManager.CanAccept checks it (J8 combined) |
| J8 | Quest availability filtering by active job | ✅ Done | QuestManager.CanAccept rejects quest if player.ActiveJobId ≠ RequiredActiveJobId; both WPF and console use QuestManager |
| J9 | Knowledge quest reward: grant Knowledge XP to matching job | ✅ Done | `JobKnowledgeRewardJobId/Amount` on Quest; Quest.GrantRewards calls JobManager.GrantKnowledgeXp — applies in both WPF and console |
| J10 | Fame quest reward: grant Fame XP to matching job | ✅ Done | `JobFameRewardJobId/Amount` on Quest; Quest.GrantRewards calls JobManager.GrantFameXp (active-job guard is internal) |
| J11 | Daily Fame tick (passive while active) | ✅ Done | `CharacterJob.LastFameTickDay`; JobManager.ApplyDailyTicks grants +5 FameXp once per game-day to active job; DayAdvanced wired in WPF App.xaml.cs + Console Game.cs |
| J12 | Skill decay: slow daily loss if not used that day | ✅ Done | `CharacterJob.LastSkillUsedDay` stamped in GrantSkillXp; ApplyDailyTicks subtracts up to 50 XP daily if not used, never below level floor |
| J13 | Knowledge decay: XP toward next level drains to 0 (no level loss) | ✅ Done | ApplyDailyTicks sets KnowledgeXp = TotalXpToReach(currentLevel) each day |
| J14 | Fame decay: XP and levels both decay when not active | ✅ Done | ApplyDailyTicks subtracts ~0.5 % FameXp daily (min 1) when job is not active; can reduce level |
| J15 | Wire Skill multiplier into crafting output | ✅ Done | WPF CraftLocal + server already done; console CraftMenu rewritten to use CraftingService + apply CraftQuality via GetSkillMultiplierFromXp |
| J16 | Wire Skill multiplier into upgrade output | ✅ Done | WPF UpgradeLocal + server already done; console UpgradeMenu now applies skill quality before TryUpgrade and uses npc.MasterJobId |
| J17 | Wire Skill multiplier into gather output | ✅ Done | GatherService.Gather applies ApplyGatherMultiplier to item.StackSize and grants 10 Skill XP; server already done |
| J18 | Wire Knowledge into gather daily limit | ✅ Done | Room.AddGatherBonus added; WPF + console subscribe to DayAdvanced and apply knowledge bonus each new day + once at session start |
| J19 | Wire Knowledge into upgrade level cap | ✅ Done | WPF UpgradePanelViewModel already uses KnowledgeMaxUpgradeLevel; console UpgradeMenu now uses npc.MasterJobId for knowledge check |
| J20 | Wire Knowledge into crafting recipe unlock | ✅ Done | WPF CraftPanelViewModel already filters; console CraftMenu rewritten to load from CraftingService + filter by knowledge level |
| J21 | Job quest daily repeat cap (max once per day) | ✅ Done | QuestManager.CanAccept already enforces RepeatDailyLimit; data authors must set RepeatDailyLimit = 1 in quests.json for job quests |
| J22 | Multiple job masters per job (data completeness) | ✅ Done | Added NpcType values (Woodcutter/Miner/Herbalist/Alchemist/Cook), locale keys, 5 new NPC entries across all 3 npcs.json files, GatheringMasterMenu in console; code uses MasterJobId fallbacks throughout |
| J23 | Fame sell bonus for player-to-player sales | ✅ Done | `JobManager.GetCharacterSellReceipt(seller, agreedPrice)` added; buyer pays agreedPrice, seller receives agreedPrice × active-job Fame multiplier; MP5/MP6/MP7 must call this when implemented |

---

## Quest System — Detailed Design

What is already done: kill/item objective tracking, multi-page dialog with named speakers (`npc` / `player` / `npc:<id>`),
repeatable quest infrastructure (RepeatRecord, daily/total/level caps), prerequisite chains, XP/gold/item rewards on return,
accept-dialog + return-dialog paging, QuestList page with Active/Available tabs, abandon support.

| # | Item | Status | Notes |
|---|------|--------|-------|
| Q1 | Multi-page dialog with speaker indication | ✅ Done | DialogLine.Speaker resolved by QuestDialogPanelViewModel.ResolveSpeaker |
| Q2 | Repeatable quest infrastructure | ✅ Done | RepeatRecord, daily/total/level limits, QuestManager.CanAccept |
| Q3 | Prerequisite quest chains | ✅ Done | PrerequisiteQuestIds checked in CanAccept |
| Q4 | Talk-only quest auto-complete on accept | ✅ Done | `Quest.IsTalkOnly` computed property added; WPF QuestDialogPanelViewModel + console NpcInteractionHandler both set Status = Completed immediately on accept when no kill/item objectives |
| Q5 | QuestList page: hide non-repeatable quests from Available tab | ✅ Done | `UpdateMode()` Available branch + `AvailableCountLabel` both filter to `q.IsRepeatable`; non-repeatables only accepted in person at NPC |
| Q6 | QuestList page: block returning non-repeatable quests | ✅ Done | `IsRepeatable` added to `QuestListItemVm`; `IsSelectedQuestReturnable` hides button for non-repeatables; `ReturnQuest` command guards with `if (!active.IsRepeatable) return` |
| Q7 | QuestList page: repeatables only shown after first completion | ✅ Done | `HasBeenCompletedBefore()` helper added; gates `AvailableCountLabel`, `UpdateMode()`, `IsSelectedQuestReturnable`, and `ReturnQuest` command — first run always requires the NPC |
| Q8 | Quest entity: `AcceptItems` field (items granted on accept) | ✅ Done | `AcceptItems` added to Quest + Clone(); `Quest.GrantAcceptItems(player)` method added; called in WPF QuestDialogPanelViewModel + console NpcInteractionHandler on accept |
| Q9 | Quest entity: `RequiredActiveJobId` field | ✅ Done | Shared with job system (J7/J8); already implemented — `Quest.RequiredActiveJobId` field + `QuestManager.CanAccept` check |
| Q10 | Quest entity: `RequiresParty` / `RequiredPartySize` field | ✅ Done | Fields added to Quest + Clone(); `QuestManager.CanAccept` checks both; `partySize` param threaded through `GetAvailableForCharacter` + `GetAcceptableForNpc`; console passes `ConsoleHubClient.CurrentPartyMembers.Count` |
| Q11 | Quest entity: job aspect level requirements | ✅ Done | `RequiredAspectJobId`, `RequiredSkillLevel`, `RequiredKnowledgeLevel`, `RequiredFameLevel` added to Quest + Clone(); `QuestManager.CanAccept` checks all three via `JobXpService.GetLevel` |
| Q12 | Quest entity: class requirement | ✅ Done | `RequiredClass` (`CharacterClass?`) added to Quest + Clone(); `QuestManager.CanAccept` checks `player.Class == RequiredClass` |
| Q13 | Quest entity: race requirement | ✅ Done | `RequiredRace` (`CharacterRace?`) added to Quest + Clone(); `QuestManager.CanAccept` checks `player.Race == RequiredRace` |
| Q14 | Quest list groupings (Main / Side / Faction) | ✅ Done | Implemented as scoped: a new `QuestCategory` enum (`Myria.Lib.Enums/QuestCategory.cs`, `Main`/`Side`/`Faction` - a plain closed enum like `QuestStatus`/`RoomRequirementType`, not a mod-extensible string-constant class like `ItemRarity`/`GatheringType`, since nothing calls for player/mod-added categories here) plus a `Quest.Category` property (default `Side`, included in `Clone()`) so any quest data that omits the field - existing entries, or a future external `IGameDataSource` implementer (e.g. Myria.Mono/Story) - still groups sensibly with zero changes needed there. `QuestManager.LoadQuests(string)` now passes a `JsonStringEnumConverter(CamelCase)` option to the deserializer (same pattern `RoomService.LoadRooms` already uses for `RoomRequirementType`) so `"Category": "main"` round-trips as a readable JSON string instead of a raw enum ordinal. Tagged the 10 existing `co1_*` intro-chapter quests (`co1_first_day` through `co1_hound_pack`) as `"main"` in `quests.json` - the only in-repo quests that read as a critical-path storyline by name/structure; the other 48 (12 job quests + 36 monster-hunt/area quests) were left at the `Side` default. No quest currently maps to `Faction` - there's no NPC-faction/reputation system in the codebase yet (guilds are player-created orgs, not a fit) - the category exists and renders correctly whenever a future faction quest sets it, just has nothing to show yet. Client-side: `QuestListItemVm` gained `Category`/`CategoryOrder` (int sort key: Main=0/Side=1/Faction=2, independent of the localized label so group order never depends on string comparison)/`CategoryLabel` (localized header text, new keys `pg.quests.category.{main,side,faction}` in both `locales-wpf/en.json` and `de.json`); `QuestListPageViewModel` wraps `Quests` in a `CollectionViewSource.GetDefaultView` with a `SortDescription` (by `CategoryOrder`) and a `PropertyGroupDescription` (by `CategoryLabel`), exposed as `GroupedQuests` - since it wraps the same `ObservableCollection`, it stays in sync automatically across every `UpdateMode()` clear/repopulate (tab switch, language change) with no extra refresh code. `Page_QuestList.xaml`'s `ListBox.ItemsSource` switched from `Quests` to `GroupedQuests` with a `GroupStyle`/`GroupItem` template added for the section-header visual (Georgia font, gold-soft text, matching the page's existing styling) - `SelectedItem` binding to `SelectedQuest` needed no change, since WPF group headers aren't part of the selectable item stream. Build clean (both Myria.Wpf and Myria.Server.Realm, which shares `Myria.Lib.Core`); full test suite green (Realm 65/65 - `RealmDbFixture`'s `GameService.InitializeGame()` genuinely re-parses the edited `quests.json` with the new enum converter on every test run, so this isn't just a compile-time check - Wpf 64/64). Live UI verification not attempted this pass (would need the WPF client's screen) - relying on the build + the real end-to-end JSON parse the test suite already exercises, plus this being a mechanical use of standard WPF `CollectionView` grouping (the exact shape the TODO entry itself suggested) |

---

## Class System — Detailed Design

What is already done: 12 classes (enum + ClassProfile stat-growth table), class bonuses wired into player derived stats
(`ExtraSTR/DEX/END/INT/SPR`, `ExtraBaseHealth/ExtraBaseMana`), class XP tracked per class in `Character.ClassXp`,
daily XP penalty for inactive classes (`ClassManager.ApplyDailyPenalty`, 500 XP/day), class level + progress bar shown
in the NPC ClassPanel, race-based class restrictions, `SkillFactory.GetSkillsFor` filters by `player.Class`.

| # | Item | Status | Notes |
|---|------|--------|-------|
| CL1 | Class selection in character creation | ✅ Done | `ClassOptionVm` added; race picker → `RebuildClasses()`; class picker XAML section added to `Page_CharacterCreation.xaml` (multiplayer-only, respects `ClassManager.GetAllowedClasses(race)`); preview shows class name |
| CL2 | Class level shown on character sheet | ✅ Done | `ClassLevelLabel`, `ClassXpFraction`, `ClassXpText` added to `CharacterPageViewModel`; header shows "Class Level N" after class name; class XP bar added below player XP bar in `Page_Character.xaml` |
| CL3 | Class switch 1-week cooldown | ✅ Done | `Character.LastClassChanged` added; `ClassManager.CanChangeClass` / `GetClassChangeCooldownRemaining` / `ClassChangeCooldown` added; `SetClass` enforces cooldown + stamps timestamp; WPF ClassPanel shows cooldown notice + greys button; console NpcInteractionHandler checks cooldown with days-remaining message |
| CL4 | Skill removal / hiding on class switch | ✅ Done | `ClassManager.SetClass` now removes all base `Skill` objects belonging to the old class from `player.Skills`, strips `Regular` `SkillSlot` entries that pointed to those skills, then calls `SkillFactory.UpdateSkills` to grant the new class's base skills (level-gated). Combined/Composite slot cleanup is CL5. |
| CL5 | Combined / composite skill cleanup on class switch | ✅ Done | `Character.StashedCombinedSkills` and `Character.StashedCompositeSkills` (`Dictionary<CharacterClass, List<...>>`) added; `ClassManager.SetClass` stashes old-class combined+composite skills, clears `ActiveCompositeSkillIds`, removes their `SkillSlot` entries, then restores previously stashed skills for the new class |
| CL6 | Class groups + partial level transfer on in-group switch | ✅ Done | `ClassGroup` enum added (Physical / RangerRogue / Mage / DivineHybrid); `ClassManager.GetClassGroup()` maps all 12 classes; `SetClass` grants 50% of old class XP to new class when groups match, 0% cross-group; WPF ClassPanel shows group badge per card + transfer info notice |
| CL7 | Separate XP curve for class levels | ✅ Done | `ClassXpService` created (`n × 5000` per level, cap 50); `ClassManager.GetClassLevel` now uses `ClassXpService.GetLevel`; `ClassPanelViewModel.ClassOptionVm` and `CharacterPageViewModel` swapped from `JobXpService` to `ClassXpService` for class XP progress/formatting |
| CL8 | Quest: class requirement field | ✅ Done | Implemented as Q12 |
| CL9 | Quest: race requirement field | ✅ Done | Implemented as Q13 |

---

## Map System — Detailed Design

What is already done: world-map BFS layout, zone group nodes (city/cave/forest/dungeon collapsed), current-room highlight,
pan + zoom (mouse wheel + drag), zone group-view when player is inside a big area, level/quest room gating.

| # | Item | Status | Notes |
|---|------|--------|-------|
| M1 | World map BFS layout | ✅ Done | Direction-based grid, zone collapsing, cascade-correct for bridge corridors |
| M2 | Pan / zoom | ✅ Done | Mouse-wheel zoom around cursor, drag to pan, zoom buttons |
| M3 | Group view for big areas | ✅ Done | When player is inside a zone, shows that zone's rooms with adjacent zone group nodes |
| M4 | Diagonal edges on world map | ✅ Done | Confirmed resolved |
| M5 | Map layout consistency across starting rooms | ✅ Done | Confirmed resolved |
| M6 | Click on group node → open area interior map | ✅ Done | Confirmed resolved |
| M7 | NPC tooltip on node hover | ✅ Done | Fixed 2026-09-10: the old RenderTransform/hit-test theory was wrong — the node label `TextBlock` was a separate `Canvas` sibling manually overlapped dead-center on top of the `Border` that owns the `ToolTip`, so hovering the visible label text (the natural hover spot) intercepted the hover before the Border ever saw it. Fixed by making the label the Border's actual `Child` instead (`IsMouseOver` bubbles from a visual child to its ancestor in WPF), which also incidentally makes group-node click/cursor more reliable regardless of where on the node the mouse lands. Confirmed working live |
| M8 | Background color from theme | ✅ Done | Color.Map.Node.* + Brush.MapBackground keys added to both Light.xaml and Dark.xaml; LocalMapControl.xaml uses DynamicResource Brush.MapBackground; Redraw() resolves node fill colors via TryFindResource with hardcoded fallbacks |

---

## In Progress / Needs Work

| # | Feature | Status | Notes |
|---|---------|--------|-------|
| 15 | Multiplayer — server combat validation | ✅ Done | Doc audit 2026-09-10: MP11/MP13/MP14 (group combat engine, server encounter, client sync) are all done — this row was stale (said "group combat + full client wiring missing"), see MP section |
| 16 | Party system — client UI | ✅ Done | Doc audit 2026-09-10: `Page_Game.xaml` has a full party panel (member list w/ HP+mana, leader indicator, Kick/Transfer Leader/Leave commands) — this row was stale (said "no WPF party management page"); it's an in-game panel rather than a standalone page, see MP10 |
| 17 | Party-based room gating | ✅ Done | Corrected 2026-09-10 (user clarification): the no-op is intentional, not a gap — `RoomService.CanEnterRoom`'s Party case is only ever reached from Wpf singleplayer (`ViewModel_PageRoom.cs`) and Mono (`WorldDataService.cs`), neither of which has parties at all, so there's nothing to check there by design. Multiplayer's real party gate lives separately in `GameHub.JoinRoom` (`party.GetPartyId(displayName) is not null`) and is already fully enforced server-side. Reworded the misleading `TODO` comment in `RoomService.cs` to explain this instead of reading like an open gap |
| 18 | Character stat initialization | ✅ Done | Corrected 2026-09-10 (user discussion): the original framing was wrong — `Create()`'s flat `10 + raceProfile.BaseStatBonus[...]` is correct by design; class growth is a separate, dynamically-recomputed layer (`Character.ExtraSTR` → `ClassManager.GetClassBonusForStat`, `growth × classLevel`, and class level starts at 1 not 0) already applied everywhere the effective stat matters (`TotalSTR`, `MaxHealth`, combat math) from the moment of creation. The real bug was narrower: `CharacterPageViewModel`'s character-sheet STR/DEX/END/INT/SPR display (`BaseStatsVm`) showed only `base + added`, omitting the class bonus — so the sheet showed a lower number than both the creation-page preview promised and combat actually used. Fixed by adding the class bonus into `BaseStatsVm`/`Fmt` as a labeled third component (e.g. `16 (+4 class)`) |
| 19 | World data — caves / dungeons / forests / cities | ✅ Not a defect | Corrected 2026-09-11 (user discussion): checked `caves.json`/`dungeons.json`/`forests.json`/`cities.json` directly — they're zone-grouping metadata for the world map's zone-collapsing feature (anchor room + member room IDs, see M3), not room content itself, so their 7-18 line size was never a "stub" in the sense of an incomplete format. The rooms they reference are already fully fleshed out in `rooms.json` (descriptions, exits, monster encounter tables, gathering spots). The real state: only 1 cave (8 rooms), 1 dungeon (9 rooms), 1 forest (8 rooms), and 4 cities exist total — a content-volume question (how much of the world is built out), not a code defect. Reclassified rather than "fixed"; authoring further areas is open-ended game-design/content work or a separate future ask, not a bug-fix task |
| 20 | Multiplayer — real-time game state sync | ✅ Done | Doc audit 2026-09-10: this row predates the entire Multiplayer System section below (MP1–MP21) — combat, trade, party, guild, chat, quests, equip/inventory/stats are all synced live via `GameHub`; stale, superseded |
| 21 | Multiplayer — lobby / matchmaking UI | ✅ Done | Doc audit 2026-09-10: `Page_LobbySelection.xaml` + `ViewModel_LobbySelectionPage`/`LobbySelectionService` already list/join/reload lobbies — this row was stale (said "no WPF page for joining or browsing sessions") |

---

## Multiplayer System — Detailed Design

What is already done: JWT auth, character save/load, SignalR hub (GameHub), room presence
(`JoinRoom` / `CharacterEntered` / `CharacterLeft` / `RoomCharacters`), chat channels (global / room / party / whisper),
party create/invite/accept/decline/leave with auto-leader on creation and auto-transfer on leader leave
(`PartyService`), friends send-request/accept/remove REST API (`FriendsController`, `Friendship` table),
server-side 1v1 combat (`StartCombat`, `CharacterAttack`, `CharacterCastSkill`), server-side gathering/crafting/upgrade
with job XP.

**Room presence / social UI**

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP1 | Show players in same room (client UI) | ✅ Done | Server already broadcasts `RoomCharacters` / `CharacterEntered` / `CharacterLeft`; client WPF needs a panel or overlay in the Room page listing current players with their names; this list is also the entry point for direct trade and party invites |
| MP2 | Friend request via chat right-click menu | ✅ Done | Server `POST /api/friends/request` already exists; client chat UI needs a right-click context menu on player names with "Send Friend Request" that calls the API |
| MP3 | Block player — server enforcement | ✅ Done | No `BlockedCharacters` table. Need: DB migration + `Blocked` model, REST endpoints (block/unblock/list), enforce in `GameHub.SendMessage` (whispers), `InviteToParty`, and `FriendsController.SendRequest` so blocked players cannot reach the blocker |
| MP4 | Block player — client UI | ✅ Done | Right-click context menu on player name in chat (alongside friend request): "Block Character"; blocked players' messages hidden client-side as well |

**Trading**

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP5 | Direct player-to-player trade | ✅ Done | `TradeService` state machine (Proposed → Active → BothReady → Completed/Cancelled), hub methods, atomic item/gold swap with J23 fame bonus. WPF: `TradeViewModel` + `Page_Trade.xaml`, room-presence context menu. Console: `TradeCommands.cs`, hub events in `ConsoleHubClient` |
| MP6 | Character shop — server | ✅ Done | `PlayerShopService` (renamed from `CharacterShopService` at some point — corrected here 2026-09-10), DB-backed via `AppDbContext`. Hub methods: `OpenShop`, `CloseShop`, `GetMyShop`, `DepositShopItem`, `WithdrawShopItem`, `SetShopItemPrice`, `UnlistShopItem`, `BrowseShop`, `BuyFromShop`. J23 fame bonus on sales. `ShopOpened`/`ShopClosed` broadcast to room |
| MP7 | Character shop — client UI | ✅ Done | WPF: `Page_PlayerShop.xaml` + `PlayerShopViewModel` (renamed from `Page_CharacterShop`/`CharacterShopViewModel` — corrected here 2026-09-10) (owner/buyer modes). Room context menu "Visit Shop". Console: `ShopCommands.cs` with open/close/list/add/remove/visit/buy. Hub events wired in both clients |

**Party management**

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP8 | Kick player from party | ✅ Done | `PartyService` has no `Kick` method. Add `Kick(leaderName, targetName)` to `PartyService`; add `KickFromParty(targetName)` hub method that verifies caller is the leader, removes target, notifies party. Target receives a `KickedFromParty` event |
| MP9 | Transfer party leader manually | ✅ Done | `PartyService` auto-transfers on leave but has no manual transfer. Add `TransferLeader(partyId, newLeader)` to `PartyService`; add `TransferPartyLeader(targetName)` hub method (leader-only). All members receive `PartyUpdated` with the new leader |
| MP10 | Party management client UI | ✅ Done | No WPF party management page. Needs a panel (accessible from the ingame menu) showing member list with HP/mana bars (already broadcast via `UpdatePartyStats` / `PartyMemberStats`), leader crown indicator, and buttons: Kick (leader only), Transfer Leader (leader only), Leave |

**Group combat**

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP11 | Multi-entity combat engine (MyriaLib) | ✅ Done | `GroupCombatEncounter`: N players vs M monsters, round-robin turns, all living players get XP, loot to first living player, quest progress for all |
| MP12 | PvE targeting restriction | ✅ Done | `SkillTarget.SingleAlly` added to enum; `GroupCombatEncounter.CharacterCastSkill` routes ally skills to Characters list, enemy skills to Monsters list |
| MP13 | Group combat — server shared encounter | ✅ Done | `GroupCombatService` (singleton) maps partyId → `GroupCombatEncounter` + connId → partyId; hub methods: `StartGroupCombat`, `GroupCharacterAttack`, `GroupCharacterCastSkill`; broadcasts `GroupCombatStarted/Updated/Finished` to party group |
| MP14 | Group combat — client sync | ✅ Done | WPF: `GroupCombatantVm`, group panel in `Page_Fight.xaml`, `IsGroupCombat`/`GroupCharacters`/`GroupMonsters`/`IsMyTurn` in `ViewModel_PageFight`, `StartGroupFightCommand` in `ViewModel_PageGame`, "Group Fight" button in `Page_Game.xaml`; Console: `CombatCommands` group commands (`group fight`, `attack <n>`, `cast <skill> <n>`, `group status`), events wired in `Game.cs` |

**Server-authoritative validation audit (2026-08-08)**

Goal is correctness/desync fixes, not anti-cheat hardening. Audited every public `GameHub.cs` method against both clients.
Solid (server computes, both clients synced, no action needed): `Heal`, `JoinRoom`, `SendMessage`, `Gather`, `Craft`, `Upgrade`,
`BuyFromNpcShop`/`SellItemToNpc` (WPF only — see MP15), `StartCombat`/`CharacterAttack`/`CharacterCastSkill`, all `GroupCombat*`,
all Party methods, Trade (`ConfirmTrade` re-validates both sides at swap time), Character Shop, Guild (server side, DB-backed).

Two independent tracks — Track A (console parity) is mechanical and high-value; Track B (mirror-method authority) is a design
decision about how much server-side re-verification the alpha needs yet.

*Track A — console client parity*

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP15 | Console: NPC shop / guild hub wiring | ✅ Done | `ConsoleHubClient` gained `BuyFromNpcShopAsync`/`SellItemToNpcAsync` (wired into `NpcInteractionHandler.TryBuy`/`SellMenu`, await-then-gate) and full `Guild*Async` wrappers + event listeners; new `GuildCommands.cs` (invite/inviterookie/accept/decline/leave/kick/promote/demote/transfer/disband/hire/promoterookie/fire) registered in `CommandRouter`. Guild had no console concept at all before — this is a new feature, not just wiring. Builds clean. **Needs a live two-session test** (see verification note below) |
| MP16 | Console: stats/equipment/skills hub wiring | ✅ Done | `ConsoleHubClient` gained `SyncStatAllocationAsync`, `EquipItemAsync`, `UnequipItemAsync`, `UseItemAsync`, `SlotSkillAsync`/`UnslotSkillAsync`/`ReorderSkillSlotAsync`/`CombineSkillsAsync`, wired into `PlayerCommands.AllocStat` (fire-and-forget) and `InventoryCommands`/`SkillCommands` (await-then-gate for equip/unequip/use/slot, fire-and-forget for unslot/reorder/combine since server does no independent validation there). Builds clean |
| MP17 | Console: quest/job/class/heal/session hub wiring | ✅ Done | `ConsoleHubClient` gained `QuestActionAsync`, `ToggleJobAsync`, `ChangeClassAsync`, `HealAsync`, `SaveSessionAsync`, `AbandonCombatAsync`, wired into `NpcInteractionHandler` (quest accept/return, job toggle, class change, healer — heal now applies server-authoritative HP/mana from `HealActionResult`) and `Game.cs`/`PlayerCommands.cs` exit/logout paths. Rune wiring explicitly deferred per decision (rune-drawing class is deactivated, no local console flow exists to attach it to). `AbandonCombat` wrapper added but left unwired — no flee/abandon command exists in `CombatCommands.cs` yet; needs a minimal command added when that UI is built. Builds clean |

*Track B — mirror-method server authority*

| # | Item | Status | Notes |
|---|------|--------|-------|
| MP18 | `SyncStatAllocation` point-budget validation | ✅ Done | `GameHub.cs` — now rejects negative components and enforces `pointsSpent + unusedPoints <= totalPointsEarned` (level-derived), summed as `long` to avoid an int-overflow bypass found during a desync audit (2026-09-09) |
| MP19 | `QuestAction` objective re-verification | ✅ Done | `GameHub.cs` — return now re-checks `KillProgress`/`ItemProgress` against `RequiredKills`/`RequiredItems` (same check as `Character.ValidateQuestStatuses`) before granting rewards, instead of trusting the client's claim the quest is done |
| MP20 | `GrantRune` cost/cooldown check | ✅ Done | Turned out there's no cost/cooldown mechanic in the actual rune system to check — the real gate is "has the player officially learned the rune's core word" (`RuneDrawingViewModel.SubmitWord`'s `wordKnown` check), which the hub never re-verified, so `GrantRune(baseRuneId)` would grant any rune id to any player regardless of whether they'd drawn/learned it. Fixed by moving that check into `BaseRuneService.GrantBaseRune` itself (the one call path both the client's local mirror and `GameHub.GrantRune` share), not the hub, so both stay in sync automatically. Low real-world impact — the RuneMage class that uses this is currently deactivated (no in-game way to reach it) — but closes the gap for whenever it's reactivated |
| MP21 | `EquipItem`/`UnequipItem` stat re-derivation | ✅ Done | Desync half already fixed (`EquipItem`/`UnequipItem` echo back which item id is in each slot via `CharacterUpdateDto.Equipment` → `Character.ApplySyncedEquipment`). For the remaining anti-cheat half, investigated where "the client's stat math" could actually still be trusted for something gameplay-affecting, since `Character.MaxHealth`/`TotalPhysicalAttack`/etc. are already always-live-computed properties (never a stored/client-suppliable value) reading from server-validated inputs: `SwapEquipment` already resolves equip targets against the character's own real inventory + `IsUsableBy` class check (no fake/unowned items equippable), `SyncStatAllocation` (item MP18) already caps invested stat points to what the character's level actually earned, and item 54 already validates every inventory item resolves via `ItemFactory` with a real `MaxStackSize`. The one real gap that survived all of that: `CharactersController.Save`'s `CurrentHealth`/`CurrentMana` fields were only lower-bounded (item 54's `[Range(0, int.MaxValue)]`) with no upper bound tied to what the character's own Stats+Equipment+Class/ClassXp actually derive as `MaxHealth`/`MaxMana` — a crafted save request could set either to an arbitrary huge number and have it persist as-is, a direct "free effective HP/MP" power cheat. Fixed by having `CharactersController.Save` reuse `SqlCharacterRepository.ReconstructCharacter` (widened from `private` to `internal` for this, rather than risk a second, lighter reimplementation subtly drifting from the original — `MaxHealth`/`MaxMana` depend on class-level-derived bonuses via `ClassManager.GetClassBonusForStat`/`GetClassHpBonus`, not a fixed formula computable from `Stats` alone, so a shortcut version was a real risk of drifting wrong) to build a throwaway `Character` from the fully-populated `record` right before the final save, then `Math.Min` clamp `CurrentHealth`/`CurrentMana` against its real `MaxHealth`/`MaxMana`. **Verified live** against the real Test1 character on the running servers via direct `curl` calls with a real JWT (same threat model as items 53/54 - bypassing the game client entirely): submitted a save with `currentHealth`/`currentMana` both set to 999999999 - the request was accepted (200, since the DTO-level check only requires non-negative) but the value that actually persisted was Test1's real 386/198, not the submitted number. This also directly confirms the reconstruction's correctness for a real, non-trivial character (Level 7 Fighter with class bonuses and gear) - had the formula been wrong, Test1's own legitimate value would have been mis-clamped too, but it landed exactly right; a follow-up unmodified re-save also round-tripped cleanly. Build clean, full 357-test suite green (including the existing `SqlCharacterRepositoryTests.cs` suite, confirming the visibility-only change to `ReconstructCharacter` didn't alter its behavior), Realm server rebuilt and redeployed |

**Verification note (MP15–MP17, 2026-08-08):** All changes verified by clean `dotnet build` after each part
(`MyriaRpgConsole.csproj`, 0 errors) — new call sites follow the exact same `IsConnected`-gated
`InvokeAsync<T>` pattern already proven in production for `Craft`/`Gather`/`Upgrade`. A live two-client
end-to-end run (register test account, create character, exercise each action, confirm server-side
persistence via reconnect, verify guild broadcasts land on a second session) was attempted but not
completed — the console's interactive login/character-creation prompt sequence didn't script cleanly via
piped stdin in the time available, and further scripting was descoped rather than burning excessive time
on harness fragility. **Recommend a manual live pass** (run `MyriaServer` + two `MyriaRpgConsole`
instances) before flipping this off the radar for good, particularly for: guild invite/accept across two
sessions, quest return after a fresh reconnect, and the healer's new server-authoritative HP/mana apply.

---

## Polish / Bugs / Cleanup

| # | Item | Status | Notes |
|---|------|--------|-------|
| 22 | Rename `ViewmModel_CaracterCreationPage.cs` | ✅ Done | Doc audit 2026-09-10: file is already correctly named `ViewModel_CharacterCreationPage.cs` (no typo'd file exists on disk) — this row was stale |
| 23 | Rename `QuestListPageViewModelcs.cs` | ✅ Done | Typo: ".cs" embedded inside filename. Renamed to `QuestListPageViewModel.cs` (matching its `QuestListPageViewModel` class, `git mv` to preserve history) — SDK-style project globbing meant no `.csproj`/XAML references needed updating. Build clean, full 357-test suite green |
| 24 | Skill usage during combat | ✅ Done | Doc audit 2026-09-10: `Page_Fight.xaml` has a full skill grid (`ItemsControl` over `Skills`, each bound to `CastSkillCommand` with a tooltip) alongside the Attack button — this row was stale (said "combat skill-selection UI incomplete") |
| 25 | Multiplayer — anti-cheat / server authority | ✅ Done | Stale/superseded by the MP-section audit below: combat itself is already server-authoritative (`CombatEncounter`/`GroupCombatEncounter` run server-side, `GameHub` returns the results). MP18/MP19/MP20/MP21 are all now done — see the Multiplayer System section |
| 26 | Localisation for everything | ✅ Done | Full audit 2026-09-10: cross-referenced every literal `Localization.T("...")`/`[LocalizedKey("...")]` call site (433 keys) and every data-driven locale-key reference (items/monsters/rooms/quests/jobs/npcs/classes/races Name/Description fields, 773 refs) across Wpf + Lib against `en.json`/`de.json` (merged Lib+Wpf-specific). Found exactly one real gap: item `stone` (`items.json`) referenced `item.stone`/`item.stone.desc`, present in neither locale file — added both (EN+DE), matching the tone of sibling materials like `item.wood`/`item.iron_ore`. Everything else (all 433 code keys, all 773 data-driven keys, including the `class.*`/`race.*`/`npc.service.*`/`pg.skills.type.*` dynamic-key families) was already fully covered — the doc's "missing mainly in console" framing was stale (console really is done per its own plan) and the general "here and there in wpf" framing overstated a gap that came down to one item. Also found and fixed two unrelated real bugs while auditing: (1) `room.royas.desc`'s German entry was the English text copy-pasted, never actually translated — now translated; (2) a large cluster of German room name/description entries (Hydea/Grelmito/Nuvmito/Whispering Woods/Mountain Pass/Xarra/Hollow Crypt — ~39 entries) had every ä/ö/ü/ß and em dash corrupted to a literal `?` (an encoding mishap at some point), rendering as visibly broken text (e.g. "Stra?e" instead of "Straße") — reconstructed all of them from context. Also found `Myria.Server.Realm/Data/locales/de.json` has the same corruption, but confirmed the server never calls `Localization.T`/`Load` anywhere in its own code — that file is unused dead weight with zero player-visible effect, left as-is (not worth touching for this) |
| 27 | NPC shop — sell doesn't reach server in multiplayer | ✅ Done | Fixed via uncommitted working-tree changes found already in progress: `MultiplayerInventoryGridViewModel.SellAmount` now overrides to call `GameHubService.SellItemToNpcAsync` before applying the local sell, and `ShopPanel.xaml.cs` now instantiates `MultiplayerInventoryGridViewModel` (not the base) when `GameHubService.IsConnected`. Verified `MyriaRPG.csproj` builds clean. Known minor follow-up: after server success it still applies the local sell via `base.SellAmount` → `Inventory.SellItem` (re-derives price via `JobManager.GetSellValue` rather than using `result.TotalGain` directly like the buy path does) — kept as-is because bypassing `SellItem` would also skip the `ItemSold` event that feeds the buyback list; not a correctness bug since both sides use the same formula, just duplicated computation. Confirmed working by manual WPF click-test (buy and sell both verified in a live multiplayer session). |
| 28 | Character selection — new character missing after creation | ✅ Done | Root cause found 2026-09-10 (user-reported repro: create a character on a server/realm account, then leave the session back to character selection without a full relogin): `ViewModel_CharacterCreationPage.Create()`'s server-token branch saved the new character to the server but never added it to `UserAccountService.CurrentUser.CharacterNames` — the in-memory list `Page_CharacterSelection` reads from — unlike the offline branch right below it, which already did `CharacterNames.Add(...)`. Only a full relogin re-fetches that list from the Auth server, which is why it "fixed itself" that way. Fixed by adding the same `CharacterNames.Add(character.Name)` call to the server branch |
| 29 | Character-per-account-per-realm cap (max 5) | ✅ Done | Fixed 2026-09-10: added shared `UserAccount.MaxCharacters = 5` constant. Server: `CharactersController.Save` rejects a *new*-character insert past the cap with a friendly `Conflict` (naturally per-realm since each realm has its own DB); updates to existing characters are unaffected. Client: `Page_CharacterSelection`'s `Create` command now disables once `CharacterNames.Count >= MaxCharacters` (covers both online and offline/singleplayer, since both read the same in-memory list); `ViewModel_CharacterCreationPage.Revalidate()` re-checks the same cap defensively (covers offline saves, which have no server to reject them, plus stale-state edge cases) and shows a new localized `pg.character.create.validation.max_reached` message |
| 30 | Realm selection navigation — wrong target / login page full-screen | ✅ Done | Doc audit 2026-09-10: `ViewModel_LobbySelectionPage.BackAction` now calls `Navigation.Current.ClearFrame(NavigationFrameType.Startup)` specifically to avoid pushing a full-screen `Page_Login`, with a comment describing this exact fix — this row was stale (feature is called "Lobby Selection" in code, not "Realm Selection") |
| 31 | Console — split singleplayer / multiplayer login (mirrors WPF) | ✅ Done | Console previously conflated the two: `Login`/`Register` read/wrote a local `Data/users/{username}.json` file *and* opportunistically also tried a server login on top — registration was effectively broken for multiplayer since the account it created never existed server-side. Rewired to match WPF's architecture: new `LoginManager.PlaySingleplayer()` (menu option 1) uses a single hardcoded `Data/users/localUser.json`, no server call at all, straight into character selection — mirrors `ViewModel_StartupMenuPage.SingleCharacterAction`. `Login()`/`Register()` (menu options 2/3) now always talk to the server only (`ConsoleHubClient.LoginAsync`/new `RegisterAsync`), never touch a local account file — mirrors `ViewModel_LoginPage`/`ViewModel_RegisterPage`. Character select/create menu extracted into shared `SelectOrCreateCharacter()`, branches on `ConsoleHubClient.HasToken` exactly as before. Verified live end-to-end against a running `MyriaServer`: register → login → create character (race/class selection, previously impossible — see item 33) → play → exit all worked; console still has no realm/lobby-selection concept (single fixed `BaseUrl`) — out of scope, WPF-only concept per item 30. |
| 32 | `ConsoleHubClient.SaveCharacterAsync`/`LoadCharacterAsync` send/expect the wrong request shape | ✅ Done | Pre-existing bug, unrelated to item 31's login rewrite — found during live multiplayer testing of item 31 and fixed as a follow-up. Console wrapped the character as `{name, level, experience, currentRoomId, dataJson: "<self-serialized blob>"}`, but the server's real `SaveCharacterRequest`/`CharacterLoadResponse` DTOs (`MyriaServer/Models/Dto/`) are fully flat (`class`, `race`, `statStrength`, `inventoryItems`, `skillIds`, etc. as top-level fields, no `dataJson`). `SaveCharacterAsync` was silently losing everything except `name`/`level`/`experience`/`currentRoomId`; `LoadCharacterAsync` always returned `null`. Fixed by porting WPF's `ServerApiService.cs` field-by-field mapping (`SaveCharacterAsync`/`LoadCharacterAsync`, `BuildCompositeSkillDtos`/`BuildCombinedSkillDtos`) into `ConsoleHubClient.cs`: added the full mirror DTO set (`CharacterSaveDto`/`CharacterLoadDto` + 11 nested DTOs) matching the server's shape exactly, rewrote both methods to map every field (stats, equipment, money, inventory, quests, jobs, skill slots, composite/combined skills incl. stashed-for-class, runes, room gathering, class XP), and added the two post-load resolution calls console was missing (`BaseRuneService.ResolveRunes`, `SkillFusionSystem.ResolveCompositeSkills`). Removed the now-unused `_playerOpts`/`ItemConverter` machinery that only existed for the old blob-serialization approach. **Verified live end-to-end**: created a character (Rotuka Knight), saved, exited, reconnected, and reloaded it — stats/class/race all came back correctly instead of the previous crash/null. |
| 33 | Console `Data/common/` was missing `races.json`/`classes.json` entirely | ✅ Done | Discovered while testing item 31 — character creation crashed into an infinite "Select race (1-0)" loop because `RaceProfile.All`/`ClassProfile.All` were empty. WPF/`MyriaServer` get these files by linking `MyriaLib/Data/common/**` directly in their `.csproj`; `ConsoleWorldRPG` instead maintains its own hand-copied `Data/common/` file set, and these two were never added to it (unlike `caves.json`/`items.json`/etc., which were). Fixed by copying `races.json`+`classes.json` from `MyriaLib/Data/common/` into `ConsoleWorldRPG/Data/common/` and adding matching `<Content Include>` entries to `MyriaRpgConsole.csproj`. Worth auditing the rest of `ConsoleWorldRPG/Data/common/` against `MyriaLib/Data/common/` for further silent gaps — not done exhaustively here, only the two that blocked this test were found/fixed. |
| 34 | Console: register-failure message flashed past unnoticed; password shown in plain text | ✅ Done | User-reported: registering with an already-taken username "behaved as if everything had worked fine" — live-tested and the error message (`❌ That username already exists.`) was actually printing correctly, but `Register()` returned straight to the login menu redraw with no pause, so the message could scroll past unnoticed in a real terminal. Added `Press Enter to continue...` after the result message. Also fixed a real, separate gap found while in this code: password entry used plain `Console.ReadLine()`, echoing the typed password in cleartext. Added a `ReadPassword()` helper (`LoginManager.cs`) using `Console.ReadKey(intercept:true)` to mask input with `*` (with backspace support), falling back to plain `ReadLine` when input is redirected/piped (masking is impossible there anyway, and `ReadKey` throws if there's no real console). Wired into both `Register()` and `Login()`. |
| 35 | WPF: monster-drop loot not applied to client inventory until relog | ✅ Done | User-reported: after winning a fight, dropped items don't appear in the inventory UI or the item-received log line, though the drop genuinely happened server-side (visible after relog). First fix attempt targeted the wrong path (`CombatTurnResult`/`ApplyCombatTurnResult`, the old solo `StartCombatAsync` route) — turned out the room page always fights via `StartGroupCombatAsync(roomId, soloOnly: true)` (`ViewModel_PageRoomMultiplayer.cs:23`), so solo and party fights both go through **group combat**, whose DTO (`GroupCombatSnapshot`) had no loot field at all. Real fix: added `GroupCombatEncounter.LootGrantedByCharacter` (mirrors the existing `XpGrantedByCharacter` dict, populated in `HandleMonsterDeath` alongside the existing server-side `Inventory.AddItem` call); added `LootItemIds` to the `GroupCombatantState` DTO (`GameActionResults.cs`); `GameHub.BuildCharacterStates` now populates it from the encounter; `ViewModel_PageFightMultiplayer.ApplyServerXpGain` (the group-combat-finished handler) now resolves each id via `ItemFactory.CreateItem` and calls `character.Inventory.AddItem(...)`, same pattern as the XP mirroring right below it. The original solo-path fix in `ApplyCombatTurnResult` was left in place too (harmless, correct if that path is ever reached from elsewhere). **Confirmed working by manual WPF click-test.** Follow-up fixed 2026-09-09 (desync audit): `GroupCombatEncounter.HandleMonsterDeath` added one `lootList.Add(drop.Id)` per `drop.StackSize` instead of once per drop, so a kill dropping 2+ of the same item now transmits/grants the full count client-side instead of always 1 (server-side inventory already had the correct count — only the client mirror undercounted). No DTO/client changes needed since `LootItemIds` was already "one entry = one unit" on the reading side. |
| 36 | Auto-update download races when two app instances overlap | ✅ Done | User reported "install didn't do anything" after 0.2.11 released — `Data/Misc/update.log` on the installed machine showed two `Checking for updates...` entries logged within the same second, both `UpdateService.CheckForUpdatesAsync` calls downloading to the exact same shared temp path (`%TEMP%\MyriaRPG_Setup_{version}.exe`) and racing: the second `File.Create` throws `IOException` ("used by another process"), aborting that check; the first attempt's own log trail then goes silent with no completion/failure line either (consistent with the app being closed mid-download, e.g. an impatient relaunch). Root cause is almost certainly **no single-instance guard** in `App.xaml.cs` — nothing stops two `MyriaRPG.exe` processes running at once (a stray double-launch, or relaunching before a previous instance has fully exited), and each independently runs its own startup-time update check against the same shared file. Same failure mode reproduced identically back on the 0.2.6→0.2.7 update (see log history) — not new, just newly noticed. Fixed the crash symptom: `UpdateService.cs` now suffixes the temp download path with `Environment.ProcessId`, so two concurrent checks (from any cause) can never collide on the same file. **Not fixed**: the actual root cause (no single-instance enforcement) — flagged for the user to decide whether it's worth adding, since it's a bigger behavioral change (would need to define what a second launch attempt does: focus the existing window? refuse to start? forward args?). This fix ships in the next release; anyone still on 0.2.10/0.2.11 needs either a successful non-overlapping auto-update or a manual install of the release that contains it. |
| 37 | Auto-update: visible pre-launch check window + cross-instance coordination | ✅ Done | Follow-up to item 36 — the update check ran silently after `MainWindow` was already shown, and while item 36 fixed the temp-path *crash*, it didn't stop overlapping checks from happening at all. Added: `Services/UpdateCoordinator.cs` (new) — a session-local named `Mutex("MyriaRPG_UpdateCheck")`; whichever instance acquires it immediately becomes the "leader" and performs the real check, any other instance ("follower") waits up to 90s (via `Task.Run`-wrapped `WaitOne` so it never blocks the UI thread) for the leader to finish and then just proceeds — no duplicate network hit/download, no race. Handles `AbandonedMutexException` (a crashed leader) as a successful acquire so it can never permanently wedge future launches. `UpdateService.CheckForUpdatesAsync` now takes an `IProgress<UpdateProgress>` and reports `Checking`/`Downloading` (with real percent via a manual buffered-copy loop against `Content.Headers.ContentLength`, replacing the old fire-and-forget `GetStreamAsync`)/`LaunchingInstaller`/`UpToDate`/`Failed`, and returns `bool` (installer launched) so the caller knows whether to keep going. New `View/Windows/UpdateCheckWindow.xaml(.cs)` — small themed window (reuses `Brush.Background`/`Brush.GoldSoft`) shown for both leader and follower for consistent UX (follower just shows the same "checking" spinner while it waits, since it doesn't know the leader's real progress). `App.xaml.cs`: `OnStartup` is now `async void` so the flow can be awaited without freezing the UI; settings/localization/theme/asset-dictionary loading already ran earlier in the existing step order so the window can show real localized/themed text; the update flow runs after those steps but before race/class/rune/job data loading and `MainWindow` creation. Had to add `ShutdownMode = ShutdownMode.OnExplicitShutdown` around the window (default `OnLastWindowClose` would otherwise shut the whole app down when the update window closes, since `MainWindow` doesn't exist yet) — restored to `OnLastWindowClose` right after `MainWindow.Show()`. New localization keys `update.checking`/`update.downloading`/`update.restarting` (EN+DE, `MyriaLib/Data/locales/`). **Not in scope** (flagged, same as item 36): true single-instance *enforcement* — two full game sessions can still run side-by-side; this only prevents the update-check itself from racing. Confirmed working by manual WPF launch test before the v0.2.12 release that shipped it. |
| 38 | New character starts with wrong current HP/MP (higher HP, lower MP than max) | ✅ Done | User-reported 2026-09-10, repro: Race Iymva, Class Archer → 280/225 HP, 125/165 MP (both should start full). Root cause: `Character`'s constructor snapshots `CurrentHealth = MaxHealth; CurrentMana = MaxMana;`, but every character-creation call site builds the object as `new Character(name, stats) { ..., Class = chosenClass }` — the object initializer's `Class` assignment runs *after* the constructor body, so the snapshot is taken while `Class` is still its property default (`Fighter`), using Fighter's HP/MP growth instead of the actually-chosen class's. Confirmed exactly: Fighter's growth reproduces 280 HP precisely by hand-calculation. `Myria.Mono` already had an independent workaround for this exact issue in its own `CharacterCreationScreen`; applied the same re-sync (`character.CurrentHealth = character.MaxHealth;`/`CurrentMana = MaxMana;`, placed after Class/gear are final) to `ViewModel_CharacterCreationPage.Create()` (Wpf) and `LoginManager.CreateNewCharacter()` (Console) — the two call sites that were missing it. Load paths (`ServerApiService`/`ConsoleHubClient`) were already unaffected since they explicitly set `CurrentHealth`/`CurrentMana` from the saved DTO via their own object initializers |
| 39 | Attribute stat display unreadable once class bonus is shown (Ref 18 follow-up) | ✅ Done | User-reported 2026-09-10: the verbose `"18 (+2, +4 class)"` format from item 18's fix didn't fit the ATTRIBUTES panel's value column (fixed 48px in a 252px-wide sidebar) and got visually clipped down to only the class figure. Fixed properly this time: the main number (`STR_Display` etc.) keeps the original short `"N"`/`"N (+added)"` shape (proven to fit) with the class bonus folded into `N` so it's still numerically correct; the class contribution is shown as a separate, short `"+N"` `Run` in a distinct gold color instead of spelling out the word "class" (`BaseStatsVm.STR_ClassText` etc., bound as a second `Run` in `Page_Character.xaml`). Also widened the value column 48px → 70px (label column is `Width="*"`, absorbs the difference) and added `TextTrimming="CharacterEllipsis"` as a safety net. Verified via build + full test suite only — could not get live visual confirmation of this specific fix: the WPF app doesn't respond to synthetic `PostMessage` clicks (likely relies on real input for hit-testing), and driving it further would have required either moving the real cursor (risking clicks landing on another foreground window) or a full UI Automation harness, so this one needs a manual look |
| 40 | Skill combination errors all show as "already exists" | ✅ Done | User-reported 2026-09-10 (Bug): existing skill combinations either don't get shown, or a *different* combination error also gets reported as "combination already exists" — the error path doesn't distinguish the actual failure reason. First pass (client-side, real but not the root cause): added a client-side recipe pre-check to `SkillCombinationViewModel.Combine()`/`MultiplayerSkillCombinationViewModel.Combine()` (shows a distinct "no recipe" message before ever asking the server) and changed `GameHubService.CombineSkillsAsync` from `Task<bool>` to `Task<bool?>` so `MultiplayerSkillCombinationViewModel.CombineAsync` can tell "never reached the server" (`null` → "not connected") apart from "server said no" (`false` → "duplicate") — both real gaps, kept. But live-testing on a **freshly created** character with zero prior combinations still showed "already exists" for a genuinely new pair (Venomous Shot + Volley) on the very first attempt ever, which none of the above explains. Root cause (found via `[DIAG]` logging temporarily added to `GameHub.CombineSkills`, later removed): `Myria.Server.Realm` did not link its game-content data from the canonical `Myria.Lib.Core/Data` the way `Myria.Wpf.csproj`/`Myria.Console.csproj` already do (see their own `Content Include ... LinkBase="Data\common\"` — a comment on the Wpf one even says this was done specifically "so this project can no longer drift out of sync with MyriaServer's copy the way races.json/classes.json/skills.json previously did", but the fix was apparently never carried over to the Realm server itself). Instead `Myria.Server.Realm` kept its own hand-copied `Data/common/*.json` snapshot that had silently drifted: `classes.json`, `items.json`, `monsters.json`, `npcs.json`, `races.json`, `shops.json`, `skill_combinations.json`, `skills.json`, and both locale files all differed from the canonical copy (older schema, missing the newer `Effects`/`AggroModifier` fields), and `effects.json` was **missing entirely** — `EffectFactory.LoadEffects` degrades gracefully for a missing file (logs an error, loads zero definitions) rather than crashing, so every status effect (poison/stun/silence/buffs/debuffs/lifesteal/regen) was silently inert whenever resolved through the Realm server. Specific to this bug: the Realm's stale `skills.json` granted Archers `aimed_shot` ("Aimed Shot") as their 2nd starting skill, while the canonical/client data has `venomous_shot` ("Venomous Shot") in that slot — the client displayed "Venomous Shot" (resolved from its own correct copy) but the character's server-side skill list actually held `aimed_shot`, so combining it sent an id the server didn't recognize as owned; `SkillCombinationService.TryCreateForCharacter` rejected it, and that bare rejection is what the client showed as "already exists". Fixed structurally, not just resynced: `Myria.Server.Realm.csproj` now links `Data/common/**/*.json` and `Data/locales/**/*.json` straight from `..\Myria.Lib\Myria.Lib.Core\Data`, exactly like Wpf/Console, and the old physically-duplicated files were deleted (kept `Data/common/gameStatus.json`, genuinely Realm-local live save state, and `Data/guild_config.json`/`Data/common/rooms.json`, Realm-specific and already identical to the canonical copy) — this class of drift is now structurally impossible, not just fixed for today. **Confirmed live end-to-end**: rebuilt the Realm server, relaunched against a fresh account/character with zero prior combinations, and the exact previously-failing pair (Venomous Shot + Volley) now succeeds ("Created: Venomous Volley") on the first attempt. Full 362-test suite still green after the fix. |
| 41 | Skill slot assignment reverts after a moment, only sticks after a full relog | ✅ Done | First attempt (2026-09-10, the `EstablishServerSessionAsync` awaited-ordering fix below) turned out to be real but **not** the actual cause the user hit — confirmed by live repro: built UI Automation tooling (`System.Windows.Automation`, since synthetic `PostMessage` clicks don't register with this app) to actually drive the client against real local Auth+Realm servers, plus temporary `[DIAG]` logging in `GameHub.SlotSkill`. Server log on the very first slot attempt for a **freshly created** level-1 Archer: `slots=1/1 atCap=True already=True` — the server already considered the one skill slot full (with `quick_shot`) before the client had done anything, while the client showed `0/1`. Root cause: `SkillSlotService.MigrateIfEmpty` (auto-fills slots from an empty list) is called by every *load* path (`ServerApiService.LoadCharacterAsync`, `SqlCharacterRepository.LoadAsync` used by `GameHub.LoadCharacter`, `ConsoleHubClient`'s load) but was never called by any client-side *creation* path (Wpf, Console, or Mono). So a freshly created character's client-side copy starts with 0 slots used, while the server's independent load/migration of that same character (the moment `GameHub.LoadCharacter` attaches a session) ends up with slot 1 already auto-filled — a real, persistent mismatch from the very first second, not a timing race. Every subsequent slot attempt failed because the server was already at cap, and only a relog fixed it because *that* path (`ServerApiService.LoadCharacterAsync`) does call `MigrateIfEmpty`, converging both sides on the same migrated state. Fixed by adding `SkillSlotService.MigrateIfEmpty(character)` to `ViewModel_CharacterCreationPage.Create()` (Wpf) and `LoginManager.CreateNewCharacter()` (Console) — Mono skipped, it has no multiplayer yet so this specific desync can't occur there. **Confirmed live end-to-end**: registered a real account, created a character, watched the slot count read `1/1` correctly on first load (matching the server), removed it, slotted a different skill, confirmed via server log it was accepted (not rejected) and it held after a 5s wait. The earlier awaited-ordering fix (`EstablishServerSessionAsync`) is still in place as a legitimate hardening against a real (if secondary) race, just wasn't sufficient on its own.<br><br>**Third round (still 2026-09-10), user-reported**: after the above fix, the initial migrated skill showed correctly, but unassigning it and assigning a *different* skill brought the same revert-then-relog-reverts-again behavior back, with the *old* (unassigned) skill winning after relog rather than the new one. Confirmed by live repro (same UI Automation harness, `[DIAG]` logging added to `SlotSkill`/`UnslotSkill` too) that the individual Unslot/Slot hub calls themselves were fine — the real bug was in how the character gets saved on exit. `ViewModel_GameWindow.SaveAsync()` (called by "Main Menu"/"Save and Quit"/the standalone Save button) did a **client-side REST save** of `UserAccountService.CurrentCharacter` (its own local mirror) as a separate step from `DisconnectAsync()`, which triggers the **server's own** `OnDisconnectedAsync` auto-save of its authoritative session character - two independent, non-atomic writes to the same row, over two entirely different channels (HTTP REST vs SignalR) with no ordering relationship to each other. If a very recent fire-and-forget slot-assignment confirm hadn't been fully reflected in the client's local mirror yet (or vice versa) when the REST save fired, whichever save happened to land last in the DB would silently win, regardless of which one was actually current. Fixed by dropping the redundant REST save entirely and routing `SaveAsync()` through the hub's own `SaveSession` (`GameHubService.SaveSessionAsync()`) instead - the same call `OnDisconnectedAsync` already uses, saving the server's one authoritative session character rather than a second, independently-drifting copy. Bonus: since SignalR processes invocations from one connection in order, awaiting this hub call also naturally waits for any earlier fire-and-forget confirm on the same connection to land first, closing the ordering gap too. Applied the same fix to Console (`Game.SaveCharacter`, `PlayerCommands`' inline "logout" duplicate, and widened `ConsoleHubClient.SaveSessionAsync` to return the hub's actual success/failure instead of discarding it) - Console had the identical redundant-REST-save pattern, just accidentally usually-masked by an even-later disconnect-triggered save downstream. **Confirmed live end-to-end** with the exact adversarial sequence (unslot → immediately slot a different skill → immediately hit Main Menu with no waiting, repeated 3 times against a live server): the server log showed every Unslot/Slot pair succeeding in order, and after each full relog the *newly*-slotted skill (not the old one) was what came back |
| 42 | Quest description text wraps/cuts off at the wrong position | ✅ Done | User-reported 2026-09-10 (UI mismatch): quest descriptions don't line-break at the correct position, so part of the text gets cut off where the text box ends. Root cause: the description preview `TextBlock` in `Page_QuestList.xaml`'s quest-details hero block was bound through a custom `TruncateAtWordConverter` that manually inserted a single `\n` near character 100 (a hand-rolled, font/width-blind approximation of word wrap) but never set `TextWrapping="Wrap"` on the `TextBlock` itself — so anything past that one forced break rendered on a single unwrapped line and ran off the edge of the box, visually "cut off". Fixed by dropping the converter entirely (bind directly to `SelectedQuest.Description`) and adding `TextWrapping="Wrap"`, letting WPF's real layout engine wrap at the actual rendered width instead of a fixed character count. Deleted `TruncateAtWordConverter.cs` (now dead — it had exactly one call site) along with its 5 unit tests in `ConverterTests.cs`. **Confirmed live** in a singleplayer session: both a short ("First Day in Hydea") and longer ("Marsh Viper Infestation") quest description now wrap cleanly across multiple lines with nothing cut off. Full test suite green after the fix (357/357, down from 362 — the 5 removed dead tests) |
| 43 | Quest title text wraps/cuts off at the wrong position | ✅ Done | User-reported 2026-09-10 (UI mismatch): same line-break/cutoff issue as item 42, but for quest titles. Root cause was the same class of bug in a different spot: the quest-list row's title `TextBlock` (`Page_QuestList.xaml`, inside the `ListBox.ItemTemplate`) had no `TextWrapping` or `TextTrimming` at all, unlike its subtitle sibling right below it which already had `TextTrimming="CharacterEllipsis"` — a long title (e.g. "Marsh Viper Infestation") would overflow past the title column's right edge and visually run into/under the fixed-width Level column next to it. (The quest-details panel's larger title, which already had `TextWrapping="Wrap"`, was unaffected — only the compact list-row title was missing it.) Fixed by adding `TextWrapping="Wrap"` to that `TextBlock`, matching how item 42's description now wraps. **Confirmed live**: "Marsh Viper Infestation" now wraps onto two lines cleanly in the quest list without overlapping the level badge |
| 44 | NPC shop items show no hover description/stats | ✅ Done | User-reported 2026-09-10 (needed feature, QoL): items for sale in any NPC shop don't show a hover tooltip with description/stats, unlike inventory items which already have this. Investigation found the feature was actually already fully built — `ShopPanel.xaml` already had a `ShopItemTooltipPopup` bound to `CurrentShopTooltip`/`IsShopTooltipVisible`, `ShopPanelViewModel` already had working `ShowShopTooltipCommand`/`HideShopTooltipCommand` methods reusing the exact same `ItemTooltipViewModel` (description, rarity, stats, class-compatibility color) that inventory items use, and the shop item template already wired `MouseEnter="ShopItem_MouseEnter"`/`MouseLeave="ShopItem_MouseLeave"`. The actual bug: `ShopItem_MouseEnter` in `ShopPanel.xaml.cs` checked `sender is Border border`, but the shop item template's root element (`ShopPanel.xaml`'s `ListBox.ItemTemplate`) is a `Grid`, not a `Border` — a `Grid` is never a `Border` in C#'s type system, so that `if` silently failed on every single hover and the tooltip command was never invoked. (The working inventory-item version, `InventorySlot_MouseEnter`, does the same `is Border` cast correctly because its own item template root really is a `Border`.) Almost certainly a copy/paste of the inventory handler that never got its type check updated for the shop template's different root element. Fixed by widening the cast from `Border` to `FrameworkElement` (covers both templates, and any future template shape) in `ShopItem_MouseEnter`. Live UI-Automation testing via synthetic `SetCursorPos`-driven hover could not reproduce the popup appearing in screenshots even after the fix — turned out to be a harness limitation, not a code issue: WPF `Popup` content renders in its own separate native window, invisible to `PrintWindow`-based capture of the main window handle, so even the pre-existing/already-working inventory tooltip screenshotted the same way (blank) as a sanity check. **User confirmed live** that both the shop tooltip (e.g. "Iron Sword" — Rarity/Type/class compatibility/ATK+DEX bonus) and the inventory tooltip work correctly after the fix, screenshotting them directly. Full test suite green (357/357) |
| 45 | XP bar doesn't update after winning a 1v1 fight | ✅ Done | User-reported 2026-09-10 (Bug), found during a post-fix playtest: after killing a monster in a normal (non-group) 1v1 fight, the XP bar doesn't refresh to reflect the XP just gained. It only catches up once some unrelated action triggers a general UI refresh elsewhere (e.g. visiting the Healer). Investigation confirmed the WPF client's plain "Fight" button is *not* a separate solo combat system — per item 35's own history, it already runs through the same `GroupCombatEncounter`/`GameHub.StartGroupCombat(soloOnly: true)` engine group fights use, client and server code paths byte-for-byte identical. Root cause: the HUD's XP bar (`CharacterHeaderVm` in `ViewModel_PageGame.cs`) only re-renders in response to four of the character's own events — `XpGained`, `LeveledUp`, `HealthChanged`, `ManaChanged` — but the method multiplayer combat actually uses to apply a server-authoritative XP result, `Character.ApplySyncedProgress` (`Character.cs:246`), only ever fired `LeveledUp`, and only when the kill happened to cross a level boundary. A same-level XP gain (the overwhelmingly common case, and the only one reachable at low level per the user's own note) updated `Experience` in memory correctly but raised nothing the HUD was listening for, so the bar stayed stale until an unrelated `HealthChanged`/`ManaChanged` incidentally fired later (exactly the "visiting the Healer" symptom). Group fights only *looked* unaffected because a multi-participant fight's finishing snapshot almost always carries an HP delta too (damage taken since the player's last synced turn), which happens to trigger the same incidental refresh — not because the group path is structurally different. Fixed by having `ApplySyncedProgress` also fire `XpGained` (with the exact, correctly-computed amount) whenever `Experience` changed without a level-up; the level-up case was already fully covered by the existing `LeveledUp` firing, so it's left untouched. `XpGained`'s `Amount` also feeds a "+X XP" room-chat log line (`ViewModel_PageRoom.OnXpGained`) that a rough/approximated amount would have made misleading across a level boundary — deliberately scoped the fix to only the same-level case, where the delta is always exact, rather than reconstructing a lossy estimate across skipped levels. **Confirmed live**: fresh level 1 character, plain "Fight" (not "Group Fight") against a Marsh Viper, no level-up — XP bar visibly filled to 24% and the room log showed "You gained 12 XP" immediately on the win screen, with no Healer visit or other action needed. Full test suite green (357/357) |
| 46 | Group fight pulls in party members from other rooms | ✅ Done | User-reported 2026-09-10 (Bug), found during the same playtest: starting a group fight pulls in party members regardless of whether they're actually in the room the fight started in — only party members physically present in that room should join. **Root cause, same as item 47 below** (user pinpointed the actual trigger for both while continuing the playtest: "the group fight button doesn't work if another party member is already in an group fight, that shouldn't happen, either if the ongoing group fight was started in the same room the player who now presses the button should just join the fight, or if the player who now presses the button is in a different room, it should start a separate group fight for this new room"): `GameHub.StartGroupCombat` keyed every party fight purely by `partyId` — a party could only ever have **one** live `GroupCombatEncounter` system-wide, with no room dimension at all. Two independent bugs fell out of that single design gap: (1) `StartGroupCombat`'s `players` list (the actual combatants) was correctly room-filtered, but the parallel `memberConns` list (who gets registered as "in this fight" and who receives the `GroupCombatStarted` broadcast) had **no room filter** — every party member got subscribed regardless of location; (2) worse, the broadcast itself (`CombatGroupClients`) didn't even use `memberConns` — it sent to `Clients.Group(PartyService.PartyGroup(fightId))`, the party's own *persistent*, room-independent chat/notification group (populated once on party-join, never room-scoped), so literally every party member's client received `GroupCombatStarted` and opened the fight UI regardless of room. Fixed structurally: fights are now keyed by `"{partyId}:{roomId}"` instead of bare `partyId`, so a party split across two rooms can have two fully independent encounters running at once; `memberConns` now has the same room filter as `players`; and `CombatGroupClients` now sends to `groupCombat.GetConnections(fightId)` (the connections actually registered to *this* fight, tracked by `GroupCombatService`) instead of the party's broad persistent group. **Confirmed live** with two real clients in a real party: starting a fight in one room while a party member stood in a different room left that member completely undisturbed (no broadcast, no UI change) |
| 47 | Group fight button stops starting fights after a specific flee sequence | ✅ Done | User-reported 2026-09-10 (Bug), found during the same playtest. Original repro (flee group → flee 1v1 → flee group again, button then silently does nothing) turned out to be one manifestation of the same room-unaware `partyId`-only fight key described in item 46 — with only one fight slot per party system-wide, a still-registered party member anywhere (even mid-cleanup from a just-fled fight) could make every subsequent `StartGroupCombat` call for the whole party fail with `already_in_combat`, silently, no matter which room it was tried from. User later pinned down the reliable trigger directly: pressing "Start Group Fight" while *another* party member is already in a group fight always failed, regardless of room — and per the user's own spec, the correct behavior splits in two: same room as the ongoing fight → join it; different room → start an independent fight of its own. Fixed as part of item 46's structural fix (room-scoped fight keys) plus new join support: `GroupCombatEncounter.AddCharacter` (new) appends a late-arriving combatant to the end of the turn order without disturbing anyone else's already-decided turn or the encounter's speed-gauge threshold; `GameHub.StartGroupCombat` now checks `groupCombat.GetEncounter($"{partyId}:{roomId}")` before doing anything else — if a live encounter already exists for this exact party+room, the caller joins it (`AddCharacter` + `GroupCombatService.AddConnection`, a `GroupCombatStarted` sent to just the joiner with the fight's full current state, and a `GroupCombatUpdated` with a new `pg.fight.log.joined` log line sent to everyone else already in it) instead of being rejected or starting a duplicate. **Confirmed live end-to-end** with two real clients in a real party against a real server: (1) party member in a different room started their own independent fight while the first fight was still running elsewhere, unaffected; (2) a party member who walked into the same room as an already-running fight and pressed "Group Fight" joined the exact same encounter (identical monster HP on both screens), with "GroupB joins the fight!" correctly appearing in the original fighter's combat log; (3) after both fled, both could immediately start fresh fights again with no stuck state left behind. Full test suite green (357/357) |
| 48 | Group fight turn order only works for the player who started the fight | ✅ Done | User-reported 2026-09-10, found while verifying item 47's fix live: "the turn logic for the players doesn't work. Only the player who initiated the group fight gets a turn, every other player gets ignored... it looked like player 2 still didn't have any allowed action as if they were waiting for their turn still." Confirmed live with two real clients: the *second* party member's turn indicator and combat log correctly showed it was their turn, and their `CanAct`/`IsMyTurn` view-model state genuinely was `true` (verified directly, not just visually) at that exact moment — but their Attack button stayed disabled and clicking it sent nothing to the server. Root cause: `Button.Command="{Binding AttackCommand}"` together with `IsEnabled="{Binding CanAct}"` in `Page_Fight.xaml` — WPF's `ButtonBase` also syncs its *effective* `IsEnabled` from the bound `Command`'s own `CanExecute()` result, layered on top of (and able to overwrite) an explicit `IsEnabled` binding, and that sync only actually re-runs on `CommandManager`'s requery heuristic (focus/keyboard/mouse activity in that specific window) — not immediately on every property change, and effectively never for a window that isn't the foreground/focused one at that moment. `RaiseAll()` (`ViewModel_PageFight.cs`) called `OnPropertyChanged(nameof(CanAct))` and the blanket `CommandManager.InvalidateRequerySuggested()`, but never told `AttackCommand`/`CastSkillCommand` specifically that their own `CanExecute()` had changed, so the stale prior result silently won the race against the correctly-updated property binding. Fixed by explicitly calling `RaiseCanExecuteChanged()` on both commands inside `RaiseAll()`, which is synchronous and doesn't depend on window focus or requery timing. (Also cleaned up a related but separate leak found while investigating: `ViewModel_PageGame` subscribed to 15 static `GameHubService` events, incl. `GroupCombatStarted`, and never unsubscribed — its own constructor unconditionally calls `Navigation.Current.InvalidateCache(Nav.Room)`, guaranteeing every future return-to-room builds a fresh instance whose predecessor's subscriptions were never torn down. Added `Unsubscribe()`, called on the outgoing `_current` instance right before a new one takes over — real fix either way, though live testing showed the `IsEnabled`/Command desync reproduces even in a single freshly-logged-in session with no leak involved, so it was not this session's primary cause.) **Confirmed live end-to-end** with two real clients: after the fix, the second party member's Attack button correctly enabled the instant it became their turn, the click reached the server, and the attack (a miss, but a real processed turn) appeared in both clients' logs. Full test suite green (357/357) |
| 49 | Group fight win with a partial casualty leaves the dead character stuck at 0 HP | ✅ Done | User-reported 2026-09-10: "at the end of the group fight, I leveled a character to a level where at least one of the characters in the party survive the group fight... let the archer survive till the end" — implying a party member who died mid-fight while the fight still ended in a win never gets respawned. Root cause: `GroupCombatEncounter.FinishCharactersWon()` sets `CharactersWon = true` purely off `Monsters.All(m => !m.IsAlive)`, completely independent of whether every party member survived getting there — a party can "win" with one or more members dead. But `GameHub.GroupCharacterAttack`/`GroupCharacterCastSkill` only called `RespawnDeadGroupMembersAsync(encounter)` (which moves dead characters to the last healer room, fully heals them, and notifies their client) inside the `if (!encounter.CharactersWon)` branch — a character who died mid-fight while their ally finished off the last monster never reached that call at all, left standing at 0 HP wherever the fight took place, unable to fight again ("You cannot fight while defeated") until manually walked to a Healer NPC (which works around it because `GameHub.Heal()` has no alive-check gate — a legitimate separate recovery path, not the real fix). Fixed by calling `RespawnDeadGroupMembersAsync(encounter)` unconditionally after a finished fight in both methods, regardless of `CharactersWon` — the method already internally filters to `encounter.Characters.Where(p => !p.IsAlive)`, so it's a no-op for a clean win with no casualties. **Verified**: live-tested against the user's own real level-7 Fighter/level-2 Archer party on their running servers — recovered their actual pre-existing stuck-at-0-HP character via the Healer workaround, then ran 7 more live group fights with the fix deployed. Directly confirmed the fix works for a party member who dies mid-fight (correct full-HP/MP respawn to town, correct death XP penalty, correct sync to both clients' own screens and the party overlay) across multiple full-party-loss fights and one fight where only one of the two died before the other also fell (also a loss). Could not force the *exact* win-with-one-survivor outcome live in the time available — the game's monster aggro locks onto one target for the whole fight and only shifts after that character dies, so a loss reliably follows close behind a first death (the survivor typically can't finish off the remaining, undamaged-by-them monster pool solo before also falling) — but the fix removes the `CharactersWon` branch distinction entirely, so the win and loss cases now call the exact same unconditional respawn code path; the loss-case confirmation is symmetric proof for the win case. Full 357-test suite green after the fix, with temporary `[DIAG2]` logging added during investigation removed before finalizing |
| 50 | Multiplayer group fight: attacking after your target dies silently does nothing | ✅ Done | Found live while verifying item 49. Root cause: `ViewModel_PageFight.cs`'s offline combat path (`SyncFromGroupEncounter`) auto-advances `SelectedMonsterIndex` to the next living monster whenever the current target dies, but the real multiplayer branch of `AttackAsync`/`CastSkillAsync` (`ViewModel_PageFightMultiplayer`, `_isLocalGroupCombat` false) never called anything that did the same — it just sent `_selectedMonsterIndex` straight to `GameHubService.GroupCharacterAttackAsync`, so once the target died every further attack kept sending the same dead index, which the server correctly rejects (`GroupCombatEncounter.CharacterAttack` returns `false` for a dead target), coming back as an empty snapshot with no visible feedback until the player manually clicked a different monster row first. Fixed by adding the identical auto-advance check (same `if (SelectedMonsterIndex < GroupMonsters.Count && !GroupMonsters[SelectedMonsterIndex].IsAlive) { ... }` shape as `SyncFromGroupEncounter`'s) to `OnGroupCombatUpdated` — the one handler both the live per-turn broadcast *and* the dead-target rejection's own existing resync fallback (`ResyncGroupCombatStateAsync` → `OnGroupCombatUpdated`) already route through, so this single change fixes both `AttackAsync` and `CastSkillAsync`: your own kill now retargets you immediately, and a rejected attack's resync retargets you before your next click instead of needing a manual retarget first. Build clean, full 357-test suite green. Purely client-side, additive logic reusing an already-proven-correct pattern from the same file - live verification not yet done, deferred alongside item 51 |
| 51 | Fleeing a group fight only removes the fleeing character, not their still-present party member | ✅ Done | Found live while verifying item 49. Root-caused: `GameHub.AbandonCombat()` (the handler behind the multiplayer "Run" button, hub method name `AbandonCombat`) only ever called `session.RemoveEncounter`/`groupCombat.RemoveConn` — unregistering the fleeing player's own SignalR connection from the fight registry — but never touched the shared `GroupCombatEncounter` object's own roster/turn order at all. If it was (or became) the fled character's turn, nobody could ever act for them again; the remaining party member's own `CharacterAttack`/`CastSkill` calls kept getting silently rejected (`IsThisCharactersTurn` never matched them), leaving their Attack button permanently disabled with no recourse except also fleeing the same already-one-member-short fight. Fixed with a new `GroupCombatEncounter.RemoveCharacter(string characterName)` (mirrors `AddCharacter`'s late-join shape in reverse, same copy-on-write list-swap pattern from item 59's fix, reuses the same `_joinLock`): removes the fled character from the roster without disturbing anyone else's already-decided turn order; if it was their turn, play advances to the next living character exactly as if they'd finished a do-nothing action (reusing the existing "next living, else monsters' turn, else `SkipDeadCharacters`" shape inline rather than refactoring the existing private turn-advance methods, to keep the change scoped and low-risk); if the last remaining character flees, the fight ends as a loss via the existing `FinishCharactersLost()` (mirrors how `MonstersTurn`/`SkipDeadCharacters` already handle "nobody left standing"). `GameHub.AbandonCombat()` now looks up the fight (`groupCombat.GetPartyId`/`GetEncounter`) *before* removing the connection (needs the fightId `RemoveConn` would otherwise erase), calls `RemoveCharacter`, then removes the connection, then broadcasts a `GroupCombatUpdated` snapshot to whoever's left (excluding the fled connection itself, which already got its own local "fled" navigation) — or just cleans up the registry with no broadcast if the fight ended because everyone left. Added a matching new localization key `pg.fight.log.fled` (EN "{0} flees from the fight!" / DE "{0} flieht aus dem Kampf!"), mirroring `pg.fight.log.joined`'s exact style, in both `Myria.Lib.Core/Data/locales/{en,de}.json`. Build clean, full 357-test suite green, Realm server rebuilt and redeployed. **Live verification deliberately deferred at the user's choice** (offered, declined for now) rather than assumed unnecessary — this one specifically involves turn-order index math across two live clients and would benefit from a real check when convenient, more so than the smaller audit fixes. See also item 68 for an adjacent, lower-severity display bug noticed while tracing this one |
| 68 | `OnGroupCombatUpdated` updates the party-roster display by array index, not identity — a shrinking or growing roster mid-fight can show stale/misaligned HP | ✅ Done | Found while fixing item 51. `ViewModel_PageFight.cs`'s `OnGroupCombatUpdated` (the `GroupCombatUpdated` broadcast handler) and `OnGroupCombatFinished` both updated `GroupCharacters` purely by positional index, silently assuming the roster's count/order never changes between updates — no longer true once items 47/51 added mid-fight join/flee support (a party member leaving earlier in the roster made every later member's HP/IsAlive get written into the wrong, now-misaligned UI row). `GroupMonsters` did NOT have this problem and was left untouched: `Monsters` is a fixed-size roster set once at encounter creation (a monster only ever goes `IsAlive=false`, never added or removed), so index-based updates are still correct there. Fixed by replacing both Characters loops with an identity-based sync (character names are globally unique per account, per the `GlobalUniqueCharacterName` DB migration): for each character in the snapshot, find the existing `GroupCombatantVm` by `RawName` and update it in place, or add a new one if it's not there yet (a mid-fight joiner); then remove any local entries no longer present in the snapshot (a mid-fight leaver). Turn-gating itself (`CurrentTurnCharacterName`/`IsMyTurn`) was never affected by the original bug — both already matched by name string, not index — so this fix is purely about the roster display now staying correctly in sync with itself. Build clean, full 357-test suite green. Live verification not yet done, deferred alongside items 50/51 (same underlying scenario - a mid-fight join or flee - so best tested together) |
| 52 | Group fight win/loss finish broadcast reaches nobody — both clients freeze on the stale mid-fight screen forever | ✅ Done | User-reported 2026-09-10, found live testing item 49's fix with a real partied Test1/Test2 session: "got the fight to the end on my own, finishing blow was already done, though the fight get's stuck there." Server log confirmed the fight genuinely resolved (`Group combat won: Test1 (Lvl 7), Test2 (Lvl 2)`, XP/loot/save all correct) but neither client transitioned out of the fight screen — further Attack clicks silently no-opped. Root cause: `GroupCharacterAttack`/`GroupCharacterCastSkill` called `groupCombat.RemoveByParty(partyId)` *before* `CombatGroupClients(partyId).SendAsync("GroupCombatFinished", snap)`. `CombatGroupClients` resolves recipients via `groupCombat.GetConnections(fightId)`, which reads the exact same connection registry `RemoveByParty` had just wiped — so the finishing broadcast's recipient list was empty by the time it was sent, reaching nobody, while `RemoveByParty` also erased both callers' `GetPartyId` mapping so every subsequent action silently returned `EmptyGroupSnapshot()`. This was a latent regression from item 46: `CombatGroupClients` used to send to the party's persistent room-independent chat group (unaffected by `RemoveByParty`), and only started depending on the dynamic per-fight connection registry once item 46 room-scoped fights — nobody had re-run this exact finish path since. Fixed by swapping the order in both methods: broadcast `GroupCombatFinished` first, then `RemoveByParty`. **Confirmed live**: rebuilt/redeployed the Realm server, recovered both stuck clients via Run/reconnect, then ran a fresh partied group fight to a clean win — both clients correctly returned to the room screen this time |
---

## Security & Robustness Audit (2026-09-10)

User requested a proactive audit for unsafe code/methods that could cause problems later, separate from live-reproduced bugs above. Four parallel agent passes covered: Realm hub authorization/input validation, concurrency/shared-state safety, persistence/save-race safety, and WPF client crash/leak risks. None of the below have been fixed yet unless marked otherwise — starting with #53.

| # | Item | Status | Notes |
|---|------|--------|-------|
| 53 | `BuyFromNpcShop` accepts negative quantity → infinite gold + corrupted inventory stacks | ✅ Done | Found in the 2026-09-10 security/robustness audit. `GameHub.cs:838-875`. Every sibling economy method (`SellItemToNpc`, `Craft`, `BuyFromShop`, shop deposit/withdraw) rejects `quantity &lt;= 0`; this one never validated `quantity` at all. A negative quantity made `totalCost = BuyPrice * quantity` negative, so the guard `Balance.BronzeTotal &lt; totalCost` never tripped (a non-negative balance is never less than a negative number), and `player.Money.TrySpend(totalCost)` (`MoneyBag.cs:30-37`) did `Balance -= Money(negative)`, i.e. **added** gold. Also set `item.StackSize = quantity` (negative) before `Inventory.AddItem`, corrupting an existing stack or inserting a negative-count item row. Repeatable on any item at any NPC, capped only by `MoneyBag.Capacity` (300,000 default). Fixed by adding `if (quantity &lt;= 0) return new NpcShopBuyResult(false, "invalid_quantity", 0, 0);` as the first line, matching every sibling method's guard exactly (`SellItemToNpc`, `BuyFromShop`, `Craft`). Client-side already has a generic fallback (`MultiplayerShopPanelViewModel.cs:72-78`'s `_ => Localization.T("npc.shop.buyFailed")`) for any reason code it doesn't specifically recognize, so no client change was needed for the new `"invalid_quantity"` reason to display gracefully. Rebuilt, full 357-test suite green, Realm server redeployed |
| 54 | `POST /api/characters` save endpoint applies every client-supplied field with zero validation | ✅ Done | Found in the 2026-09-10 security/robustness audit. `CharactersController.cs:205-396` (`SaveCharacterRequest`). `[Authorize]`-gated to the caller's own username, so not cross-player, but within that scope copied `MoneyBronze`, `MoneyCapacity`, `Level`, `Experience`, every stat, `InventoryPages`, and an arbitrary `InventoryItems` list (id + stack size, no bounds) straight onto the DB row with no server-side recompute or cap. Fixed with two layers: (1) `[Range]`/`[Required]` data annotations added to `SaveCharacterRequest`'s scalar fields (`Level` ≥1, `Experience`/`ExpForNextLvl` ≥0, `CurrentHealth`/`CurrentMana` ≥0, all 5 base stats + their bonus variants ≥0, `StatBaseHealth`/`StatBaseMana` ≥1, `MoneyBronze` ≥0, `MoneyCapacity` clamped to `(1, 300_000]` since no capacity-upgrade feature exists anywhere in the codebase today, `InventoryPages` clamped to `(1, 50]` as a generous sanity ceiling) plus `CharSaveInventoryItem.StackSize`/`SlotIndex` — all enforced automatically by `[ApiController]`'s built-in model validation (a `400` with a field-specific message, no controller code needed) before the action method even runs; (2) three imperative checks added at the top of `Save()` for what attributes can't express: every `InventoryItems` entry's `ItemId` must resolve via `ItemFactory.TryCreateItem` (rejects nonexistent items) and its `StackSize` must not exceed that item's own `MaxStackSize`; `WeaponItemId`/`ArmorItemId`/`AccessoryItemId` (if set) must also resolve to a real item; and `MoneyBronze` must not exceed `MoneyCapacity` (a cross-field constraint `[Range]` can't express on its own — caught only because live-testing this fix deliberately tried it and got an unexpectedly-still-valid 999999999999 gold through past the first two guards before this one was added). **Verified live** against the real Test1 character on the running servers via direct `curl` calls with a real JWT (bypassing the game client entirely, matching the exploit's own threat model): a negative level was rejected (400, DataAnnotations), a nonexistent item id was rejected (400, imperative check), gold exceeding the cap was rejected (400, cross-field check), and — critically — an unmodified legitimate re-save of the same character still succeeded (200) with Test1's real DB data confirmed unchanged afterward (level 7, 775 bronze, untouched by any of the three rejected attempts). Full 357-test suite green, Realm server rebuilt and redeployed. Also discovered mid-fix (unrelated to this bug): the Auth server (`MyriaAuthServer.exe`) had stopped running at some point this session and was relaunched — needed anyway to obtain a real JWT for this test, but worth the user knowing it wasn't running |
| 55 | Player-shop purchases can be duplicated — two buyers, one item, both succeed | ✅ Done | Found in the 2026-09-10 security/robustness audit (independently by two separate passes). `PlayerShopService.CommitSaleAsync` (`PlayerShopService.cs:198-212`), called from `GameHub.BuyFromShop`. The method's own comment claimed it guards against concurrent purchases, but there was no transaction, row lock, or concurrency token anywhere in the schema — plain read-check-write (`SingleOrDefaultAsync` → check `Quantity &lt; quantity` → decrement → `SaveChangesAsync()`), so two buyers hitting the last unit near-simultaneously in separate `DbContext`s could both read `Quantity=1`, both pass the check, both decrement to 0 and save — both getting the item, seller paid twice for one unit sold. Fixed without a schema migration: `AppDbContext.OnModelCreating` now configures `PlayerShopItem.Quantity` itself as an EF Core optimistic-concurrency token (`.IsConcurrencyToken()`) rather than adding a dedicated `RowVersion` column — this makes EF include the originally-read `Quantity` value in the `UPDATE`/`DELETE`'s `WHERE` clause, so a second commit against a now-stale read affects zero rows and throws `DbUpdateConcurrencyException` instead of silently succeeding. `CommitSaleAsync` now catches that exception and returns `false`, which `GameHub.BuyFromShop` already correctly handled as `"insufficient_stock"` (no client-side change needed). **Verified** with a temporary xUnit probe against the real `PlayerShopService`/`AppDbContext` (deterministic "stale read" simulation rather than real concurrent threads — genuine concurrent writes against SQLite's shared in-memory test connection hit its single-writer lock and hang without a busy-timeout, a SQLite-in-test artifact unrelated to the fix): seeded a shop with 1 unit, had two independent `DbContext`-backed buyers both validate successfully (both see stock=1), committed Buyer A first (succeeded), then committed Buyer B against its now-stale read (correctly rejected) — final stock confirmed at exactly 0, never negative, never double-sold. Probe deleted after confirming (not part of the permanent suite). Full 357-test suite green (confirming `IsConcurrencyToken()` needed no migration - "No migrations were applied" on server startup), Realm server rebuilt and redeployed |
| 56 | `TradeService` uses plain unsynchronized `Dictionary`s in a server-wide singleton | ✅ Done | Found in the 2026-09-10 security/robustness audit. `TradeService.cs:31-32` — `_pending`/`_byConn` are plain `Dictionary&lt;K,V&gt;`, no lock anywhere in the class, yet the service is registered `AddSingleton`, so these two dictionaries backed *every trade on the server at once*. Fixed by mirroring `PartyService`'s exact pattern: added a `private readonly object _lock = new();` and wrapped the body of all 9 public methods (`Propose`, `CancelPending`, `Accept`, `Snapshot`, `PartnerConn`, `AddItem`, `RemoveItem`, `SetGold`, `Confirm`, `End`) in `lock (_lock) { ... }`, including the private `Get` helper's callers (it's only ever called from within an already-locked method, documented on the helper itself). Also had `Snapshot()` return `.ToList()` copies of the offer lists instead of live references into the session's internal mutable `List&lt;TradeItem&gt;`, since a "snapshot" holding a live reference that could still be mutated after the lock is released defeats its own purpose. Kept `Dictionary` rather than switching to `ConcurrentDictionary` - with every access now serialized through the single lock, a concurrent-safe collection type would be redundant, not wrong. Confirmed all 9 public methods are lock-wrapped via a full re-grep of the file after editing. Full 357-test suite green, Realm server rebuilt and redeployed. No live multi-connection race test attempted for this one (unlike item 55's EF-concurrency-exception path, mutual exclusion via `lock` is a guaranteed language primitive, not a subtle behavior needing empirical verification - the same pattern already runs correctly in production via `PartyService`) |
| 57 | `CharacterPageViewModel` leaks a `GameHubService.HubConnected` subscriber on every character-sheet open | ✅ Done | Found in the 2026-09-10 security/robustness audit. `CharacterPageViewModel.cs:374` (constructor): `GameHubService.HubConnected += SyncStatsToServer;` with no `Unsubscribe`/`Dispose`/`Cleanup` anywhere in the class — same bug class as the `ViewModel_PageGame` leak fixed in item 48, just a different page that was missed. Fixed by porting item 48's exact `_current`/`Unsubscribe()` pattern directly into this file's own constructor (self-contained fix - no changes needed at any of the several call sites that construct a fresh instance per open): added `private static CharacterPageViewModel? _current`, `_current?.Unsubscribe()` at the top of the constructor, `_current = this` at the end, and a new `Unsubscribe()` method removing the `HubConnected` handler. Build clean, full 357-test suite green. **Live-verified functionally**: launched the real WPF client against the real running servers, logged in as Test1, and opened/closed the character sheet 4 times in a row — stats panel (STR/DEX/END/INT/SPR, Damage/Defense/M.Damage/M.Defense, XP/CL bars) rendered correctly every time with no crash or degradation. Did not attempt a live reconnect-cycle repro to directly observe the redundant-hub-call symptom disappearing (the leak's actual effect only manifests across a disconnect/reconnect, which isn't easily forced through the UI) - relying instead on this being a mechanical, self-contained port of an already-proven-correct pattern from the same codebase |
| 58 | `ViewModel_PageRoom` leaks 9 static event subscriptions on every character switch/rejoin | ✅ Done | Found in the 2026-09-10 security/robustness audit. `ViewModel_PageRoom.cs:271-279` subscribed to 8 `GameHubService` events plus static `GameLog.EntryAdded` in its constructor with no matching unsubscribe anywhere in the file — switching/rejoining a character (character-select "Join", race selection, class reselection) orphaned the old instance with all 9 subscriptions intact, each leaked `OnHubConnected` re-firing a full `EstablishServerSessionAsync()` (LoadCharacter/SetCharacterName/JoinRoom) on every future reconnect. Fixed the same way as items 48/57, but reusing infrastructure this class already had: it already carried a `private static ViewModel_PageRoom _instantce` field (pre-existing typo, left as-is) used for several "last instance wins" static callbacks, so the fix just added `_instantce?.Unsubscribe()` at the top of the constructor and a new `Unsubscribe()` method removing all 9 subscriptions - no second static field needed. Build clean, full 357-test suite green. **Live-verified by actually triggering the leak scenario**: launched the real WPF client against the real running servers, logged in as Test1, then used the in-game Settings → "Character Selection" → rejoin flow twice in a row (3 total `ViewModel_PageRoom` constructions on one connection - the exact trigger path from the audit). Room rendered correctly after each rejoin, and movement (North into Nuvmito — Valley Floor) worked correctly on the final, third instance - confirming `OnRoomCharacters`/`OnCharacterEntered`/etc. still function normally post-fix and nothing broke from the repeated construct-and-unsubscribe cycle |
| 59 | `GroupCombatEncounter.AddCharacter` races with combat-resolution enumeration over the same list | ✅ Done | Found in the 2026-09-10 security/robustness audit. `GroupCombatEncounter.cs` — `AddCharacter` (plain `_characters.Add(character)`, a `List&lt;Character&gt;`) vs. `HandleMonsterDeath`'s unsnapshotted `foreach (var p in Characters)`/`Characters.Where(...)`, neither of which is guarded against the other running concurrently on a separate hub connection (`AddCharacter` is called from `StartGroupCombat`'s join path, fully independent of `GroupCharacterAttack`/`CastSkill`'s calls into the same encounter). Fixed with a copy-on-write swap instead of a broad lock across every read site in the file: `_characters` changed from `readonly List&lt;Character&gt;` to a plain (reassignable) `List&lt;Character&gt;`, and `AddCharacter` now does `_characters = new List&lt;Character&gt;(_characters) { character };` instead of mutating the existing list in place. A C# `foreach` captures its enumerator once at the loop's start, so an in-progress `HandleMonsterDeath` enumeration keeps safely iterating the *old* list object even if `AddCharacter` swaps `_characters` to a new one mid-iteration on another thread — no `InvalidOperationException`, no corruption, and every other read site in the file (many) is covered for free without touching them individually. Also added a small `_joinLock` scoped only to `AddCharacter`'s own check-then-write, closing the narrower risk of two concurrent joiners both passing the "not already in" check before either writes (would otherwise double-add the same character - much lower severity than the main crash risk, but cheap to close in the same pass). Build clean, full 357-test suite green (had to restart the Realm server mid-session to release its lock on the old `Myria.Lib.Core.dll` before the test rebuild could copy the new one). Realm server rebuilt and redeployed. Per user's request, skipped live UI verification this round (they needed the screen for something else) — relying on the reasoning above (a well-understood, standard C# enumeration-semantics argument) plus the clean test suite |
| 60 | `ViewModel_IngameMenu.SaveAsync()` still has the dual-save race that was already fixed once elsewhere | ✅ Done | Found in the 2026-09-10 security/robustness audit (independently re-discovered by the persistence-focused audit pass too). `ViewModel_SettingsPage.cs` (class `ViewModel_IngameMenu`), `SaveAsync()`, wired to the Character Menu / Main Menu / Save &amp; Quit buttons on `Page_Settings.xaml`. Called `ServerApiService.SaveCharacterAsync(player)` (a REST POST of the client's local in-memory mirror) when online, then `MainMenuAction`/`SaveQuitAction` triggered `GameHubService.DisconnectAsync()`, which fires the server's own `OnDisconnectedAsync` auto-save of its authoritative session copy — the exact same race already found and fixed this session in the sibling `ViewModel_GameWindow.SaveAsync()`. Fixed by porting that exact fix over: `SaveAsync()` now calls `GameHubService.SaveSessionAsync()` when `ServerApiService.Token is not null` (persisting the server's own authoritative session character, the same one `SlotSkill`/`UnslotSkill`/etc. actually mutate, instead of a second independently-drifting REST write), falling back to the existing offline `CharacterService.SaveCharacter` path unchanged. Comment explicitly credits `ViewModel_GameWindow.SaveAsync` as the original fix this mirrors. Build clean, full 357-test suite green. Per user's request, skipped live verification this round (they needed the screen for something else) — this is a small, mechanical port of an already-proven-correct fix already confirmed working live in its original location (`ViewModel_GameWindow`) |
| 61 | `GuildPageViewModel.Cleanup()` has a `+=`/`-=` typo — doubles a subscription instead of removing it | ✅ Done | Found in the 2026-09-10 security/robustness audit. `GuildPageViewModel.cs:237`: `GameHubService.GuildRookieAdded += OnRookieAdded;` inside `Cleanup()`, while every other line in that method correctly used `-=`. Fixed with the one-character change (`+=` → `-=`). Build clean, full 357-test suite green. Per user's request, skipped live verification this round (they needed the screen for something else) |
| 62 | No rate limiting on any SignalR hub method or most REST controllers | ✅ Done | Found in the 2026-09-10 security/robustness audit. `Program.cs` only defined a `"public"` rate-limit policy applied solely to `PublicCharactersController`; the authenticated REST controllers and every `GameHub` SignalR method had no throttling at all. **REST half fixed earlier**: `"authenticated"` policy (60/min, partitioned by JWT username), applied via `[EnableRateLimiting("authenticated")]` to all four controllers - verified live via curl (61st-65th of 65 rapid calls got `429`). **This pass closed the hub-method half.** `GameHub` methods can't use ASP.NET Core's `AddRateLimiter`/`UseRateLimiter` (HTTP-pipeline only, no notion of individual hub invocations over an already-established connection) - built a hand-rolled `HubRateLimitFilter` (`Myria.Server.Realm/Hubs/HubRateLimitFilter.cs`) instead: a per-connection fixed-window counter (100 requests/10s, generous vs. REST's 60/min since one hub call is typically a single small user-triggered action like one attack or one chat line, not a full data fetch), throwing `HubException("rate_limited")` once exceeded - `HubException`'s message is the one exception type SignalR forwards verbatim to the client instead of masking it, appropriate here since the client is meant to see it. Buckets are removed in `OnDisconnectedAsync` to avoid unbounded growth from reconnects over a long-running server. **Registration bug found and fixed along the way**: the existing `UnhandledExceptionLoggingFilter` (added for item 66) and this new filter were both registered the way several online examples show - `services.AddSingleton<IHubFilter, T>()` with no further wiring - which compiles cleanly but **silently never gets invoked by SignalR's dispatcher** (confirmed empirically: an always-on diagnostic log line in the new filter never fired across 110 real hub invocations against a live connection). The actually-effective global-filter API turned out to be `services.AddSignalR(options => options.AddFilter<T>())` (plus registering `T` itself, not `IHubFilter`, in DI) - this compiled at first with the exact same `"No overload for method 'AddFilter' takes 0 arguments"` error documented as a dead end during item 66's original attempt, which turned out to be a missing `using Microsoft.AspNetCore.SignalR;` in `Program.cs` hiding the real (correct, zero-arg generic) `HubOptionsExtensions.AddFilter<TFilter>()` extension method the whole time, not an actual API mismatch. Added the `using`, switched both filters to the `AddFilter<T>()` form (ordered so the logging filter wraps the rate limiter, so a rate-limit rejection still gets logged the same way any other hub exception would) - meaning `UnhandledExceptionLoggingFilter` itself was **never actually active** from item 66 through this pass despite being believed to be working; it is now, for the first time, genuinely wired in. **Verified live**: wrote a throwaway console probe (`Microsoft.AspNetCore.SignalR.Client`, deleted after use, not part of the repo) that logs in as Test1 via the real Auth server, opens a real hub connection to the real running Realm server, and fires the harmless no-op `ReportMods` hub method rapidly - confirmed exactly 100 succeeded and requests 101-110 all threw `HubException("rate_limited")`, both against the diagnostic build (with the server log showing the filter fired on every one of the 110 calls and logged the rejection warning exactly 10 times) and again against the final cleaned-up build with identical results. Full four-project test suite green (Realm 65/65, Wpf 64/64, Console 33/33, Auth 50/50), Realm server rebuilt and redeployed. **This closes the last open item from the 2026-09-10 security/robustness audit (53-67, 15 findings) - all 15 now fixed, with one sub-finding (item 64's guild-name race) confirmed to already be a non-issue** |
| 63 | `GroupCombatService.AddConnection`/`RemoveByParty` has a check-then-act gap that can orphan a joining connection | ✅ Done | Found in the 2026-09-10 security/robustness audit. `GroupCombatService.cs` (`AddConnection`: checked `_byParty.ContainsKey(fightId)` then separately wrote `_connToParty[connId]`) raced with `RemoveByParty`/`RemoveConn` — same failure class as item 52 (a registry-consistency ordering gap), narrower window. Fixed by mirroring `PartyService`'s exact pattern: added a `private readonly object _lock = new();` and wrapped the composite (multi-step) methods — `Start`, `AddConnection`, `ReassignConnection`, `RemoveByParty`, `RemoveConn` — in `lock (_lock) { ... }`. Left the plain single-dictionary-read getters (`HasEncounter`, `GetPartyId`, `GetEncounter`, `GetConnections`) unlocked, since a single `ConcurrentDictionary` read is already atomic on its own and doesn't have a check-then-act gap to close. Build clean, full 357-test suite green, Realm server rebuilt and redeployed. Per user's request, skipped live verification this round (they needed the screen for something else) — mutual exclusion via `lock` is a guaranteed language primitive here, same reasoning as item 56 |
| 64 | Non-transactional multi-step writes and a check-then-insert race in guild code | ✅ Done | Found in the 2026-09-10 security/robustness audit; three related sub-findings, one of which turned out to already be a non-issue on closer check. (1) **Fixed**: `GuildPropertyService.PurchaseHouseAsync`/`EstablishBaseAsync` each did a treasury-debit-and-property-insert `SaveChangesAsync()` followed by a *separate* room-insert `SaveChangesAsync()` with no surrounding transaction — a crash between the two left a guild that paid full price for a house with zero rooms, no rollback of the treasury debit. `BuyListedHouseAsync` in the same file already had the right pattern (`db.Database.BeginTransactionAsync()`/commit/rollback); ported it to both methods. (2) **Fixed, and extended beyond the two methods the audit named**: `AllocateRoomIdsAsync` computes `next = MAX(RoomId)+1` via a plain query with no lock of its own, so two near-simultaneous callers could allocate the same `RoomId` — wrapping the read-through-commit span in a transaction closes this too (SQLite serializes concurrent write transactions), so the *same* fix was also applied to `AddHouseRoomAsync` and `BuildBaseRoomAsync`, which call the identical helper and were equally exposed even though the audit only flagged the two treasury-purchase paths by name. `BuildBaseRoomAsync`'s in-memory material deduction (documented as persisting only on next session save, independent of this DB write) was deliberately left outside the transaction - a separate, pre-existing in-memory/DB ordering question, out of scope for this specific TOCTOU fix. (3) **Checked, found already safe - not a real bug**: `GuildService.CreateGuildAsync`'s `AnyAsync` name/tag check does race with a concurrent insert, but `AppDbContext.cs` already has `e.HasIndex(g => g.Name).IsUnique()` and `e.HasIndex(g => g.Tag).IsUnique()` on the `Guild` entity (confirmed by direct inspection) - the audit's claim that "no unique DB constraint visible in the Realm schema" was inaccurate for this specific entity. A losing concurrent `CreateGuildAsync` would hit a DB-level unique-constraint exception rather than silently creating a duplicate; the only real gap is that exception isn't caught into a friendly message, a minor UX nicety rather than the data-integrity bug originally described - left as-is rather than "fixed" for something that wasn't actually broken. Build clean, full 357-test suite green, Realm server rebuilt and redeployed. Per user's request, skipped live verification this round (they needed the screen for something else) — the transaction pattern is mechanically identical to `BuyListedHouseAsync`'s, already in production use in the same file |
| 65 | Trade/Shop ViewModels only clean up subscriptions on explicit close, not on disconnect mid-transaction | ✅ Done | Found in the 2026-09-10 security/robustness audit. `TradeViewModel.cs` and `PlayerShopViewModel.cs` (`CharacterShopViewModel`) both correctly defined `Unsubscribe()`, but it was only invoked from `OnCompleted`/`OnCancelled`/`Close()` — all depending on either a server round-trip or an explicit UI click, so a disconnect (or a SignalR auto-reconnect, which gets a new connection id server-side and orphans the old trade/shop session server-side just the same) never triggered it. `GameHubService` had no dedicated "disconnected" event to hook into (only `HubConnected`, fired on both the original connect and every reconnect, and `Reconnected` internally re-raises the same `HubConnected` event) — rather than adding new plumbing, reused that: since a `TradeViewModel`/`CharacterShopViewModel` instance can only ever be constructed *after* the initial connection already succeeded, any `HubConnected` firing during its lifetime is necessarily a genuine reconnect. Both classes now subscribe to `GameHubService.HubConnected` in their constructor and route it into their existing teardown path — `TradeViewModel` reuses `OnCancelled`'s existing generic-reason branch (closes the window with a "trade cancelled" message, same as a server-cancelled trade), `CharacterShopViewModel` reuses `Close()` directly (same as clicking the Close button) — both already call `Unsubscribe()` internally, which now also removes the new `HubConnected` subscription itself. Build clean, full 357-test suite green. Per user's request, skipped live verification this round (they needed the screen for something else) — forcing a real mid-trade disconnect is also one of the harder scenarios to reliably script through UI automation, so this one in particular would benefit from a manual check when convenient |
| 66 | Almost no exception handling around DB writes in `GameHub.cs` → client/server state can silently diverge | ✅ Done | Found in the 2026-09-10 security/robustness audit. Outside `SaveCharacterReliablyAsync` and one best-effort save, `GameHub.cs` had no `try/catch` around any `SaveChangesAsync`-driven service call. A first pass added `UnhandledExceptionLoggingFilter` (`IHubFilter`, global via `AddSingleton<IHubFilter, ...>` in `Program.cs`) as a safety net that logs every unhandled hub-method exception before rethrowing unchanged - kept, still in place, but purely additive (doesn't fix the divergence itself). **This pass did the actual per-method fix.** Systematically re-grepped every `scopeFactory.CreateScope()`/`GetRequiredService<T>()` call site in `GameHub.cs` (~20+ locations) and categorized each: the `GuildService` cluster (invites/accept/leave/kick/promote/etc., ~15 methods) turned out to already be DB-first with no prior in-memory effect - the DB write *is* the action, so an unguarded throw there can't cause divergence and needed no change. The genuine anti-pattern (effect already applied in-memory, then an unguarded await into `RookieService`) was isolated to exactly 8 call sites, all three of `RookieService`'s methods (`ApplyXpBonusAsync`, `ApplyJobXpBonusAsync`, `ApplyPurchaseCommissionAsync`) applied as a secondary "rookie bonus" always layered on top of an already-succeeded primary action: `Gather`, `Craft`, `Upgrade` (job-XP bonus after the craft/gather/upgrade itself already completed), `BuyFromNpcShop` (guild commission after the item+gold already changed hands), `CharacterAttack`/`CharacterCastSkill` (solo-combat rookie XP bonus after the fight already resolved), and both `GroupCharacterAttack`/`GroupCharacterCastSkill`'s win-block (group rookie XP bonus after the encounter already finished in-memory - the most severe of the eight, since an unguarded throw there used to abort the `SaveCombatParticipantsAsync`/`GroupCombatFinished` broadcast/`RemoveByParty`/respawn sequence that follows, i.e. exactly the class of stuck-fight bug fixed in item 52, just triggered by a DB hiccup in the bonus service instead of broadcast ordering). Fixed by adding three new private helpers (`TryApplyRookieXpBonusAsync`, `TryApplyRookieJobXpBonusAsync`, `TryApplyPurchaseCommissionAsync`) that each wrap their `RookieService` call in `try/catch`, log a warning on failure, and return `0`/no-op instead of throwing - the primary action's result/broadcast always reflects what genuinely happened server-side, and only the optional rookie bonus itself is silently skipped on a transient DB failure (which self-heals: the next successful roll picks the bonus back up, nothing is permanently lost). All 8 call sites now route through these helpers instead of calling `RookieService` directly. Build clean; full test suite green across all four projects (Realm 65/65, Wpf 64/64, Console 33/33, Auth 50/50); Realm server killed, rebuilt, and redeployed with a confirmed clean startup and port bind. Live verification not attempted this pass (would need to force a `RookieService`/DB failure on demand, e.g. fault injection - not something reachable through normal play) - relying on the try/catch's behavior being a standard, low-risk C# control-flow guarantee plus the clean test suite |
| 67 | `CharacterSessionService.TryReattach` has an unlocked multi-dictionary check-then-act sequence | ✅ Done | Found in the 2026-09-10 security/robustness audit. `PlayerSessionService.cs`. The class doc claims state is "lock-protected per entry," but `TryReattach` spanned `TryGetValue` → `TryRemove` → three separate writes across `_players`/`_connectionByCharacter`/`_encounters`/`_gatherState` with no enclosing lock - two near-simultaneous reattach attempts for the same character (the method's own comment notes SignalR auto-reconnect can plausibly double-fire) could race, with the losing side falling back to a stale DB reload rather than crashing/corrupting. Fixed by mirroring `PartyService`'s pattern: added a `private readonly object _lock = new();` and wrapped `TryAdd`, `Remove`, and `TryReattach` — the three methods that move a character across multiple dictionaries as one logical operation — in `lock (_lock) { ... }`, actually making the class live up to its own "lock-protected" doc comment. Left `Get`/`GetConnectionId`/`GetEncounter`/`SetEncounter`/`RemoveEncounter` unlocked (single-dictionary reads, already atomic on their own) and `TryConsumeGather` untouched (already has its own narrower per-entry `lock (state)`). Build clean, full 357-test suite green, Realm server rebuilt and redeployed with a confirmed clean startup. Per user's request, skipped live verification this round (they needed the screen for something else) — mutual exclusion via `lock` is a guaranteed language primitive, same reasoning as items 56/63. **Audit pass status (items 53-67, 15 findings total): all 15 now fixed (62's remaining hub-throttling half closed in its own follow-up pass), and one sub-finding (the guild-name race in item 64) found to already be a non-issue on closer inspection** |
---

## Balance & Rebalancing

| # | Item | Status | Notes |
|---|------|--------|-------|
| BA1 | XP scaling by level gap | ✅ Done | `Monster.Level` field added; monsters.json assigns levels 1–20 by stat tier; `CombatEncounter` + `GroupCombatEncounter` apply `scaledXp = max(1, baseXp × (monsterLevel/playerLevel)²)` when player outlevels the monster |
| BA2 | Skill damage ignores defense | ✅ Done | `ExecuteSkillOnEnemy` / `ExecuteSkillOnMonster` now apply `raw*(raw/(raw+def))` mitigation (physical or magic defense chosen by `skill.Type`), matching the `CombatSystem.CalculateDamage` formula; minimum 1 damage |
| BA3 | Skill scaling factor audit | ✅ Done | All `ScalingFactor` values in skills.json raised to compensate for the new mitigation pass: offensive skills ~1.5×–2.2×, AoE skills 1.0×–1.2×, heals 1.0×–1.3× |
| BA4 | Character damage too high / defense too low | ✅ Done | `ClassProfile` stat growth per class level trimmed ~20–30 % across all 12 classes (e.g. Fighter STR 5→4, Barbarian STR 8→6, ArcanMage INT 8→6); HP/mana-per-class-level reduced proportionally |

---

## Content & Lore

| # | Item | Status | Notes |
|---|------|--------|-------|
| CO1 | Introductory quests (level 1–10) explaining mechanics | ✅ Done | 7-quest singleplayer intro chain added to quests.json (co1_first_day → co1_gather_ore → co1_first_ironwork → co1_goblins → co1_skill_intro → co1_troll → co1_trusted). Teaches: NPC interaction, gathering, crafting, combat, skills, harder combat. Giver NPCs: smith_default (Gorhen) and villager_lumina (Mira). Full dialog drafted for all quests |
| CO2 | Quests level 10 and beyond | ✅ Done | 11 new quests added (L10–36): ceralith_spirit_hunters, valley_wolves, treant_blight, bandit_pass, ceralith_cellar, woods_rangers, hollow_awakening, void_tide, harpy_crossings, ancient_guardians, crimson_hunt. Chain: lizzard_raids_1 → valley_wolves → treant_blight → bandit_pass → ceralith_cellar → woods_rangers → hollow_awakening → void_tide → harpy_crossings → ancient_guardians → crimson_hunt. Plus side quest ceralith_spirit_hunters (branching from lizzard_raids_1). Givers: villager_ceralith, villager_lumina, villager_default |
| CO3 | More monsters | ✅ Done | 15 new monsters added (IDs 18–32, L10–40+): Shadow Wolf, Withered Treant, Bandit Marauder, Plague Rat, Dread Archer, Hollow Knight, Void Wraith, Storm Harpy, Elder Golem, Crimson Drake (IDs 18–27) + Forest Sprite, Giant Beetle, Dire Wolf, Corrupted Dryad, Forest Witch (IDs 28–32). Room monster tables populated for rooms 14, 19–27; rooms 21–26 updated with forest monsters. Access levels adjusted: 19–20 → 13, 21–25 → 15, 26 → 18 |
| CO3b | Forest quest chain + repeatable patrol | ✅ Done | 6 forest quests added: forest_infestation, beetle_problem, dire_wolves, corrupted_heart, witch_coven (story chain) + whispering_woods_patrol (daily repeatable, L15+). NPC woodcutter_master placed in room 25 (whisperingwoods.lumina/sawmill) to give/receive these quests |
| CO3c | Job knowledge/fame quest chains | ✅ Done | 13 job-gated quests added across 4 chains: Blacksmith (job_smith_basics/forged/armored/mastery), Leathersmith (job_leather_basics/workshop/hunter), Miner (job_miner_first_haul/deep_vein/motherlode), Woodcutter (job_woodcutter_first_timber/lumber_run/forest_supply). All use `RequiredActiveJobId` + reward `JobKnowledgeRewardAmount`/`JobFameRewardAmount`. Sized to reach ~Knowledge L9 by chain end. miner_master placed in room 12 (lumina.north) |
| CO3d | Crafting recipes for all class types | ✅ Done | 12 new recipes added across 4 craft jobs: Smith (starter_gauntlets KL3, steel_sword KL15), Leathersmith (archer_garb KL3, padded_armor KL5, leather_bow KL8, hunter_coat KL10), Tailor (prayer_robes KL5, mage_robe KL8, storm_robes KL12), Artificer (mana_ring KL5, soul_shard KL8, crystal_focus_band KL12). Gated by `RequiredKnowledgeLevel` to reward job progression |
| CO4 | More gathering spots and resources | ⬜ Not Started | Expand gathering data: herbs, mushrooms, rare plants, ores, gems, wood types. New `GatheringSpot` entries in rooms.json and matching items in items.json. Each job type (Herbalist, Miner, Woodcutter) should have many more gather nodes across the world |
| CO5 | World locations from story lore | ⬜ Not Started | Existing important story locations do not exist in room/city data yet. These need to be added as rooms, cities, or area zones with appropriate NPCs, monsters, and quests. (List specific locations with Rhyen once story doc is available) |
| CO6 | Multiplayer starting area distinct from singleplayer | ⬜ Not Started | Multiplayer is set before the singleplayer story — it should start in a different location with different introductory NPCs reflecting that earlier time period. Needs: a new starting room ID for multiplayer characters (`ServerApiService.Token != null` path in character creation), different NPC set for shared-world areas, and introductory quests that fit the "before the story" era |
| CO7 | NPC and quest content for multiplayer-era locations | ⬜ Not Started | Companion to CO6 — NPCs in multiplayer areas need different names/roles/dialog that reflect the pre-story timeline. Job masters, quest givers, and shop NPCs should differ between SP and MP worlds |

---

## Language & Lore Systems

| # | Item | Status | Notes |
|---|------|--------|-------|
| LO1 | Myralic language font | ⬜ Not Started | Corrected 2026-09-10 (user-reported): a font asset already exists (`Assets/Fonts/MyriacScript.ttf`), so "no font asset exists" was wrong — it's outdated against the current Myriac script and needs to be regenerated to match. Once updated, it should be loadable as a WPF FontFamily resource; the Lexica UI (LO2) will use it for display |
| LO2 | Lexica system — in-game language learning | ⬜ Not Started | Characters should be able to discover and learn Myralic words/phrases through exploration and quests. Needs: a `LexicaEntry` model (word, translation, discovered flag), a `LexicaService` (load from JSON, track player discoveries), and a Lexica page in the ingame menu displaying discovered words in the Myralic font alongside translations |
| LO3 | Multiple in-world languages / dialects | ⬜ Not Started | The story includes different languages and dialects per region/race. Design decision needed: how many languages, which NPCs/areas use them, and how the player encounters/learns them. Mechanically: each language is a separate Lexica entry set; the player's knowledge level per language determines how much of NPC dialog is shown as Myralic glyphs vs translated text |

---

## Art & Visual Polish (WPF only)

| # | Item | Status | Notes |
|---|------|--------|-------|
| AR1 | Background images — room and UI screens | ⬜ Not Started | Most UI screens and room views still use placeholder or no background images. Needs thematic fantasy RPG backgrounds for: main menu, character selection/creation, in-game room view, combat screen, ingame menu panels |
| AR2 | Icons — replace default/missing icons | ⬜ Not Started | Items, skills, and UI buttons still using default WPF icons or no icon. Each item category (weapon, armor, consumable, material, currency) and each skill type should have a dedicated icon. Gather/craft/upgrade action buttons also need icons |
| AR3 | Game application icon | ✅ Done | Myriac-M glyph (Y/gamma shape, matching the script sheet) on themed backgrounds: WPF = deep purple + gold, Console = black + silver, Server = navy + cyan. Generated via `Tools/MakeIcons.ps1` (multi-size PNG-in-ICO: 16/32/48/256px). Wired via `<ApplicationIcon>` in all 3 .csproj files; WPF `MainWindow.Icon` set to pack URI; `Assets\App.ico` marked as `<Resource>` |
| AR4 | Theme rework — fantasy RPG aesthetic (WPF) | ⬜ Not Started | Current Light/Dark themes are generic. Rework `Assets/Light.xaml`, `Assets/Layout.xaml`, and `Assets/Icons.xaml` to use fantasy-appropriate typography, colors, and control styling (parchment textures, rune-border frames, medieval-style buttons). The color token system should be designed to port naturally to Unity UI Toolkit later |

---

## Console Version (ConsoleWorldRPG) — Feature Parity & Library Alignment

The console already references MyriaLib and shares all JSON data files.
What it lacks is (a) replacing its own duplicate logic with MyriaLib calls and (b) implementing the features
that exist in WPF but were never ported. Multiplayer parity is the final goal: console and WPF players
connect to the same MyriaServer and can play together.

**Library alignment — replace console-only duplicates with MyriaLib**

| # | Item | Status | Notes |
|---|------|--------|-------|
| CN1 | Replace `EncounterRunner` with `CombatEncounter` | ✅ Done | `CombatSystem.cs` replaced with a thin I/O wrapper; `CharacterUseItem` added to MyriaLib. Quest rewards no longer granted on kill — player must return to NPC (CN3). |
| CN2 | Replace hardcoded `NpcInteractionHandler` with `NpcService` | ✅ Done | `NpcInteractionHandler` now looks up NPCs via `room.NpcRefs`, matches by ID prefix or NpcType, and dispatches based on `npc.Type`. |
| CN3 | Quest return flow via NPC | ✅ Done | Any NPC interaction now shows returnable quests and calls `quest.GrantRewards` + moves to `CompletedQuests`. Filtering by `ReturnNpcId` pending quest data update (GiverNpcId not yet set in quests.json). |

**Feature parity — missing systems**

| # | Item | Status | Notes |
|---|------|--------|-------|
| CN4 | Race selection at character creation | ✅ Done | Numbered race picker in `LoginManager.CreateNewCharacter`; shows stats + growth per level; applies `10 + BaseStatBonus` to initial stats; sets `RaceSelected = true`; filters class list by `ForbiddenClasses`. |
| CN5 | Class system in console | ✅ Done | Class XP + level added to `status` output; `ClassMasterMenu` in `NpcInteractionHandler` mirrors `ClassPanelViewModel` (list all classes with level/XP/forbidden state, switch via number); dispatched from SkillMaster NPCs that carry `"change_class"` service. |
| CN6 | Job system UI in console | ✅ Done | `jobs` command shows all CharacterJob entries with Skill/Knowledge/Fame levels + active/cooldown state; `JobMasterMenu` in `NpcInteractionHandler` (triggered from SmithMenu option 5 when NPC has `learn_job` service) shows 3-aspect progress and toggles active job with 7-day cooldown enforcement; `jobs.json` added to console data dir; `JobManager.LoadJobs()` wired into `GameService.InitializeGame()`. |
| CN7 | Skill combinations and slots | ✅ Done | New commands: `skills` (list learned/combined/slotted), `combine` (fuse 2–5 learned skills via `SkillCombinationService`), `slots` (manage combat bar via `SkillSlotService` with slot/unslot/move). `SkillMasterMenu` now routes to these. Skill fusion deferred (no `base_skills.json` yet). |
| CN8 | Quest dialog (multi-page, speaker) | ✅ Done | `PlayDialog` + `ResolveSpeakerConsole` in `NpcInteractionHandler`; pages through `DialogLine` list with speaker prefix and "Press Enter to continue"; y/n accept/return prompt at end |
| CN9 | Character stats display | ✅ Done | `character` / `stats` commands → `Printer.ShowCharacter`: race, full stat breakdown (base + allocated + class + gear = total), unspent points warning; `alloc <stat> [n]` command to spend stat points; `ShowHelp` updated |
| CN10 | Day cycle and passive ticks | ✅ Done | `SessionStarted` wired in `Game.Start()` → `StartInactivityTimer` + `ApplyDailyPenalty`; `StartSession(realCharacter)` called after login; timer stopped on logout/exit; day+segment shown on login. Combat/gather ticks already in MyriaLib. Job decay (J12–J14) deferred |

**Multiplayer — console SignalR client**

| # | Item | Status | Notes |
|---|------|--------|-------|
| CN11 | SignalR client integration | ✅ Done | `ConsoleHubClient.cs` — REST JWT login, hub connect/disconnect, all events (ChatMessage/CharacterEntered/CharacterLeft/RoomCharacters/PartyInvite/PartyUpdated/PartyDisbanded); `say`/`whisper` in CharacterCommands; `party`/`party invite/accept/decline/leave` in PartyCommands; JoinRoom on movement; events wired in Game.cs with color output |
| CN12 | Online character save/load | ✅ Done | REST login during local auth (before char selection); server char list; `LoadCharacterAsync` (REST→Character) in `ConsoleHubClient`; server-first load with warning+local fallback; new char saved to server only; `SaveCharacter` helper branches on `HasToken` at exit/logout |
| CN13 | Multiplayer game actions via hub | ✅ Done | Route `attack`, `gather`, `craft`, `upgrade` through the hub methods (`CharacterAttack`, `Gather`, `Craft`, `Upgrade`) when online, exactly as the server validates them. Offline path keeps using `CombatEncounter` / `GatherService` directly |
| CN14 | Chat and social commands | ✅ Done | `say` (room), `g` (global), `w`/`whisper` (private), `party <msg>` (party chat); `friend list/requests/add/accept/remove` via REST. Block deferred (MP3 — no server endpoint yet). |
| CN15 | Party commands | ✅ Done | `party kick <player>` + `party promote <player>` added; `Kick` + `TransferLeader` added to `PartyService`; `KickFromParty` + `TransferPartyLeader` hub methods added to `GameHub`; `KickedFromParty` event wired in console. Invite/accept/decline/leave were already implemented. |

---

## Future / Backlog

| # | Feature | Status | Notes |
|---|---------|--------|-------|
| 27 | More race / class profiles | ⬜ Not Started | Stat profiles exist but only a few fully tuned |
| 28 | Additional quests & world content | ⬜ Not Started | Expand caves, dungeons, cities with real area data |
| 29 | PvP / player-vs-player mechanics | ⬜ Not Started | No design yet |
| 30 | Guild / social features | ✅ Done | Stale entry — guild is fully implemented: server-side `GuildService` (DB-backed, `MyriaServer/Hubs/GameHub.cs` Guild* methods), WPF `Page_Guild.xaml`/`GuildPageViewModel`/`GameHubService` Guild* wrappers, and console `GuildCommands.cs` (added this session, MP15) all exist alongside the friend list. |
| 31 | Achievement system | ⬜ Not Started | No design yet |

---

_Last updated: 2026-09-10_

