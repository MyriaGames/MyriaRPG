<UserControl x:Class="RpgClient.Controls.TopShortcutBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="150"
             Width="900">

	<UserControl.Resources>
		<SolidColorBrush x:Key="Gold" Color="#C99A4A"/>
		<SolidColorBrush x:Key="GoldSoft" Color="#E1C481"/>
		<SolidColorBrush x:Key="PanelDark" Color="#070B0F"/>
		<SolidColorBrush x:Key="PanelSoft" Color="#101820"/>
		<SolidColorBrush x:Key="ArcaneBlue" Color="#0F314D"/>

		<LinearGradientBrush x:Key="ButtonBg" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="#121D25" Offset="0"/>
			<GradientStop Color="#05080B" Offset="1"/>
		</LinearGradientBrush>

		<DropShadowEffect x:Key="Shadow"
                          BlurRadius="18"
                          ShadowDepth="0"
                          Opacity="0.65"
                          Color="#000000"/>

		<Style x:Key="ShortcutButton" TargetType="Button">
			<Setter Property="Width" Value="104"/>
			<Setter Property="Height" Value="104"/>
			<Setter Property="Margin" Value="12,0"/>
			<Setter Property="Foreground" Value="{StaticResource GoldSoft}"/>
			<Setter Property="Background" Value="{StaticResource ButtonBg}"/>
			<Setter Property="BorderBrush" Value="{StaticResource Gold}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="FontSize" Value="42"/>
			<Setter Property="Effect" Value="{StaticResource Shadow}"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
							<Grid>
								<Ellipse Stroke="#26485A"
                                         StrokeThickness="1"
                                         Opacity="0.45"
                                         Margin="16"/>

								<TextBlock Text="{TemplateBinding Content}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontSize="{TemplateBinding FontSize}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>

								<TextBlock Text="◇"
                                           Foreground="{StaticResource Gold}"
                                           FontSize="16"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Top"
                                           Margin="0,-10,0,0"/>
							</Grid>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>

			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#132A3A"/>
					<Setter Property="BorderBrush" Value="#E1C481"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>

	<Border Background="#DD070B0F"
            BorderBrush="{StaticResource Gold}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="26"
            Effect="{StaticResource Shadow}">

		<StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

			<Button Style="{StaticResource ShortcutButton}" Content="☗"/>
			<Button Style="{StaticResource ShortcutButton}" Content="♙"/>
			<Button Style="{StaticResource ShortcutButton}" Content="✦"/>
			<Button Style="{StaticResource ShortcutButton}" Content="📜"/>
			<Button Style="{StaticResource ShortcutButton}" Content="🗺"/>
			<Button Style="{StaticResource ShortcutButton}" Content="⚙"/>

		</StackPanel>
	</Border>
</UserControl>